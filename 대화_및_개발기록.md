# 📋 우리가족 보험 매니저 - 질문 & 답변 및 개발 기록 정리

> **기록 일시**: 2026년 9월 14일  
> **프로젝트**: 우리가족 보험 매니저 (`antigravity_insur`)  
> **웹앱 배포 주소**: [https://woogiiin-maker.github.io/antigravity_insur/](https://woogiiin-maker.github.io/antigravity_insur/)  
> **구글 스프레드시트 연동 URL**: `https://script.google.com/macros/s/AKfycbzcgVYAAViQNm0TFe9pUO8tdsEuLpO5G4rL8B32Ut0WqJgGTzi2SoZ4ox44ilrJsoVvGQ/exec`  
> **구글 시트 문서명**: 우리가족보험  

---

## 📌 목차
1. [프로젝트 구조 및 연동 개요](#1-프로젝트-구조-및-연동-개요)
2. [주고받은 핵심 질문 & 답변 일대기 (Q&A)](#2-주고받은-핵심-질문--답변-일대기-qa)
3. [버전별 주요 수정 및 개선 내역 (v1 ~ v8)](#3-버전별-주요-수정-및-개선-내역-v1--v8)
4. [데이터 복구 및 비상 대처 가이드](#4-데이터-복구-및-비상-대처-가이드)
5. [다른 컴퓨터나 환경에서 작업할 때 참고 사항](#5-다른-컴퓨터나-환경에서-작업할-때-참고-사항)

---

## 1. 프로젝트 구조 및 연동 개요

- **웹 애플리케이션**: React + TailwindCSS 기반의 싱글 페이지 애플리케이션 (PWA 지원).
- **데이터 저장소**: 별도의 유료 데이터베이스 서버 없이, 사용자 본인의 **구글 스프레드시트(Google Apps Script 웹 앱)**를 실시간 클라우드 DB로 사용.
- **마스터 비밀번호 보안**:
  - 비밀번호 원문은 서버에 저장되지 않고, 브라우저에서 **SHA-256 단방향 암호화 해시**로 변환되어 구글 시트 최상단 `__SYSTEM__,CONFIG` 행에 안전하게 보관됨.

---

## 2. 주고받은 핵심 질문 & 답변 일대기 (Q&A)

### Q1. "구글 시트 연동 중인데, 새 기기에서 임의로 비번을 정해도 접속되는 문제가 있어. 비밀번호는 하나여야 하지 않아?"
- **원인 분석**:
  - 초기 버전에서는 비밀번호가 접속한 기기별 브라우저 저장소(`localStorage`)에만 저장되어, 기기마다 각자 다른 비밀번호를 설정해도 자기 기기 안에서만 맞으면 열리는 구조였습니다.
- **해결 조치**:
  - 구글 시트 최상단에 마스터 비밀번호의 SHA-256 해시 행을 생성하여 저장하고,
  - 모든 기기(PC, 휴대폰 등)가 로그인할 때 **구글 시트에 저장된 공용 마스터 암호를 기준으로 실시간 검증**하도록 단일화했습니다.

---

### Q2. "GitHub 데스크탑 버전인데, 수정된 파일을 업로드(Push)하려면 어떻게 해?"
- **답변 및 조치**:
  - GitHub 데스크탑 프로그램이 PC에 설치되어 있으면, 내장된 Git 도구(`git.exe`)와 저장된 인증 토큰을 통해 **AI 어시스턴트가 명령어 한 번으로 코드를 자동 커밋 & 푸시(GitHub 배포)**할 수 있습니다.
  - 따라서 사용자분께서 일일이 웹사이트에 파일을 올리실 필요 없이 원터치 자동 배포 환경을 완성했습니다.

---

### Q3. "휴대폰에서 비번을 바꿨는데 PC에서 변경된 비번으로 로그인이 안 되고, 기존 비번으로만 접속돼."
- **원인 분석 (치명적 덮어쓰기 버그 발견)**:
  - 비밀번호 변경 함수(`aC`)에서 구글 시트로 새 비밀번호를 보내기 직전, 구글 시트의 최신 데이터를 읽어오는 과정이 있었습니다.
  - 이때 구글 시트 파싱 함수(`tx`)가 **구글 시트에 있던 기존 이전 비밀번호(`123412`)로 로컬 저장소를 덮어써버린 뒤**, 옛날 번호를 다시 구글 시트로 재전송하는 순서 버그가 발생하고 있었습니다.
- **해결 조치 (v3 배포)**:
  - `tx` 함수의 로컬 덮어쓰기 부수 효과를 제거하고, 변경된 새 암호 해시(`overrideHash`)가 구글 시트로 직접 100% 강제 전송되도록 로직을 수정했습니다.

---

### Q4. "혹시 PC나 휴대폰에 동시 접속해 있는 상태에서 비번을 바꾸면 오류가 생길 수 있어?"
- **정확한 분석 (사용자분의 직관 적중)**:
  - **네, 실제로 심각한 동시성 충돌 오류가 존재했습니다.**
  - PC에서 비밀번호를 바꿨더라도, 이미 로그인되어 열려 있던 휴대폰은 메모리에 **옛날 비밀번호**를 쥐고 있었습니다.
  - 이 상태에서 휴대폰 화면에서 데이터를 수정하거나 저장하면, 휴대폰이 기억하던 **옛날 비밀번호를 구글 시트로 다시 올려보내 구글 시트의 비밀번호를 이전 번호로 강제 롤백시키는 문제**가 있었습니다.
- **해결 조치 (v4 배포)**:
  - 구글 시트의 해시값을 **단일 진실 공급원(Single Source of Truth)**으로 설정.
  - 브라우저 포커스 및 동기화 시 구글 시트의 최신 비밀번호를 즉시 내 브라우저로 갱신하여, 옛날 비밀번호로 구글 시트를 덮어쓰는 사고를 원천 차단했습니다.

---

### Q5. "비번을 바꾸면 기존에 로그인되어 있던 기기는 자동으로 로그아웃되게 할 수 있어?"
- **답변 및 조치 (v5 배포)**:
  - 금융/보안 앱과 동일하게 구현 완료했습니다.
  - 1분 주기 백그라운드 감지 및 화면 켜짐(`visibilitychange`), 탭 전환(`focus`) 이벤트를 연동하여,
  - **어느 한 기기에서 비밀번호가 변경되면, 다른 기기들은 즉시 로그인 세션이 만료되고 화면이 잠기며** `"⚠️ 다른 기기에서 마스터 비밀번호가 변경되어 보안을 위해 화면이 잠겼습니다. 새 비밀번호로 다시 로그인해 주세요."` 알림이 표시되도록 구축했습니다.

---

### Q6. "비밀번호 변경을 누르니 '구글 시트 전송 시간이 초과되었습니다' 오류가 떠."
- **원인 분석**:
  - 구글 스프레드시트(Google Apps Script)는 수십 개 행의 보험 데이터와 암호 해시를 새로 쓰는 데 네트워크에 따라 10초 내외가 걸릴 수 있습니다.
  - 그런데 전송 대기 시간(타임아웃)이 고작 **8초**로 너무 촉박하게 잡혀 있어 브라우저가 통신을 강제로 끊어버린 것이 원인이었습니다.
- **해결 조치 (v6 배포)**:
  - 저장 대기 시간을 **8초 ➔ 35초**로 4배 이상 늘리고, 데이터 조회 시간도 **6초 ➔ 20초**로 확대하여 안정적인 저장을 보장했습니다.

---

### Q7. "우리가족보험 PDF 자료가 모두 사라졌어!"
- **상황 분석**:
  - 앞선 통신 타임아웃 오류 상황에서, 구글 시트 데이터를 다 받아오지 못한 빈 상태(`[]`)로 새 암호 행만 구글 시트에 전송되면서 스프레드시트 내용이 빈 표로 덮어씌워졌던 현상이었습니다.
- **해결 및 안심 조치 (v7 배포)**:
  1. **원본 PDF 파일**: 사용자 컴퓨터 바탕화면(`바탕화면 > 보험`, `바탕화면 > 자료 > 보험`)에 모든 원본 PDF가 안전하게 그대로 보관되어 있음을 확인.
  2. **구글 시트 복원**: 구글 시트의 **[파일 ➔ 버전 기록 ➔ 버전 기록 보기]**에서 클릭 한 번으로 1초 만에 100% 원상 복구 가능.
  3. **코드 안전 가드(Safety Guard)**: 구글 시트에서 기존 데이터를 100% 안전하게 읽어오지 못하면 **어떠한 경우에도 시트를 덮어쓰지 않고 즉시 작업을 중단**하는 방어 로직을 탑재.

---

### Q8. "휴대폰에서 뒤로가기 하면 어플이 꺼지지 말고 핵심보장 창만 꺼지게 만들어줘."
- **상황 분석**:
  - 모바일 웹/PWA 환경에서는 팝업(모달) 창이 떴을 때 브라우저 히스토리가 쌓이지 않아, 스마트폰 뒤로가기 버튼을 누르면 웹사이트/앱 자체가 닫히거나 종료되는 문제가 있었습니다.
- **해결 조치 (v8 배포)**:
  - **[핵심보장 비교표]**, **[설정 ⚙️]**, **[증권 상세 정보]**, **[수기 등록]**, **[PDF 업로드]** 등 모든 모달 창이 열릴 때 브라우저 히스토리 상태(`pushState`)를 기록.
  - 사용자가 스마트폰 뒤로가기 버튼이나 제스처를 실행하면, **어플이 종료되지 않고 열려 있던 팝업 창만 부드럽게 닫히도록 완벽 구현**.

---

### Q9. "GitHub 데스크탑 버전을 삭제하면 어떻게 돼? 꼭 있어야 해?"
- **답변**:
  - 평소에 가족 보험을 조회하고 추가/수정하는 일상 사용에는 GitHub 데스크탑이 전혀 필요하지 않습니다 (구글 시트로 100% 동작).
  - 하지만 저(AI)와 함께 코드를 고치고 새로운 기능을 업데이트(Push)할 때는 컴퓨터 안에 Git 도구가 있어야 제가 원터치 자동 배포를 해드릴 수 있으므로, **삭제하지 않고 그냥 깔아두시는 것을 권장**합니다.

---

### Q10. "이 대화 내용(AI 채팅창)이 다른 컴퓨터에 가서 로그인해도 보일까?"
- **답변**:
  - **보이지 않습니다.**
  - Antigravity AI 코딩 환경은 개발 보안을 위해 채팅 로그와 작업 트랜스크립트를 현재 컴퓨터 하드디스크(`C:\Users\cmc\.gemini\antigravity`)에 로컬로 보관합니다.
  - 다른 컴퓨터에서 로그인 시 새 대화창으로 시작되므로, **지금 이 문서(`대화_및_개발기록.md`)**를 저장소에 업로드하여 다른 기기에서도 언제든 확인하실 수 있도록 조치했습니다.

---

### Q11. "홈화면에서 암보장이나 뇌혈관 같은 다른 보험들을 누르면 핵심보장처럼 중요보험은 상위에 배치됐으면 좋겠어."
- **배경 및 니즈**:
  - 홈 화면의 카테고리 카드(암, 뇌질환, 심장질환, 수술 등)를 클릭했을 때, 기존에는 가나다순으로만 나열되어 통원비나 소액 특약이 위에 뜨고 가장 중요한 고액 일반암진단비나 뇌혈관질환진단비가 아래로 밀리는 불편이 있었습니다.
- **해결 조치 (v9 배포)**:
  - '핵심보장 분석 알고리즘'을 홈 화면 카테고리 필터링에 전격 탑재!
  - 1단계: **핵심 진단비/주요 치료비 우선순위 가중치(Priority Score)**로 자동 정렬 (`일반암/뇌혈관진단비` ➔ `특정암/뇌졸중` ➔ `주요치료비/종수술` ➔ `유사암` ➔ `통원/입원/납입지원`).
  - 2단계: 같은 보장 등급 내에서는 **보장금액(원) 높은 순**으로 자동 정렬 (5천만 원 > 3천만 원 > 1천만 원).
  - 3단계: 화면에 `👑 핵심`, `⭐ 주요` 프리미엄 뱃지를 부착하여 시각적으로도 중요 보장이 한눈에 보이도록 완성.

---

### Q12. "월납입보험료가 납입이 끝난 것들도 포함된 거 같아. 확인해줘"
- **배경 및 분석**:
  - 기존에는 납입기간이 지나 이미 납입이 종료되었거나(완납), 납입기간에 '완납'이라고 표시되어 있어도 가구 총 월보험료에 그대로 합산되어 월 납입액이 실제보다 과다하게 표시(약 173만 원)되었습니다.
  - 전수 분석 결과, 삼성생명 여성시대(5년납), 흥국생명 연금 2건(5년납/10년납), KB 무암장수(2004년 가입, 20년납 완료) 등 총 5건(약 65만 원 상당)이 이미 납입이 끝났거나 완납된 상태인 것으로 확인되었습니다.
- **해결 조치 (v10 배포)**:
  - **1. 완납 자동 인식 로직**: 납입기간 또는 비고에 '완납' 혹은 '납입완료'가 포함되어 있거나, 증권월보험료가 0원으로 설정된 경우 자동으로 월보험료를 **0원**으로 계산.
  - **2. 보장 내역 100% 보존**: 월보험료만 0원으로 처리되어 가계 지출에서 빠질 뿐, **암/뇌/심장 진단비나 수술비 등 든든한 평생 보장은 그대로 유지**.
  - **3. 직관적인 [완납] 뱃지**: 증권 목록에서 월 0원인 보험에는 산뜻한 초록색 `[완납]` 뱃지를 표시.
  - **4. 원클릭 납입완료 설정 버튼**: 증권 상세 또는 증권 월보험료 수정 창에서 **`[✅ 납입완료 (0원 설정)]`** 버튼을 누르면 즉시 완납 처리 및 복원(`[🔄 특약 합산액 복원]`) 가능.

---

### Q13. "여기 메뉴에서 다시분석, 중복정리는 필요없을거같은데 확인해줘. 완납한보험들은 완납표시가 있었으면 좋겠어"
- **배경 및 니즈**:
  - 특정 증권을 선택했을 때 상단 액션 배너에 불필요한 `[다시 분석]`, `[중복 정리]` 버튼이 공간을 차지하여 화면이 복잡하고 잘못 누를 위험이 있었습니다.
  - 또한, 완납 처리된 보험임에도 배너에 `월 0원 직접설정`이라고만 표기되어 '완납' 상태인지 한눈에 확인하기 어려웠습니다.
- **해결 조치 (v11 배포)**:
  - **1. 메뉴 정리**: 증권 선택 바에서 `[다시 분석]`, `[중복 정리]` 버튼을 완전히 제거하여 간결하고 필수적인 관리 버튼(`[보험명 수정]`, `[완납 설정]`, `[증권 삭제]`, `[닫기]`)만 남김.
  - **2. 증권 배너 완납 표기**: 완납된 보험은 `월 0원` 대신 눈에 띄는 초록색 **`[✅ 완납 ✏️]`** 버튼으로 표시 (클릭 시 언제든 월보험료 수정 가능).
  - **3. 상단 드롭다운 완납 뱃지 부착**: 증권 선택 상자(`[피보험자] [보험사] 상품명`) 및 목록 항목에 초록색 **`[완납]`** 뱃지를 달아 드롭다운을 열었을 때도 완납 보험을 즉시 구분 가능.
  - **4. 보장 테이블 및 모바일 카드 완납 표기**: 목록 내 월보험료 컬럼에도 완납된 특약은 `0원` 대신 산뜻한 **`[완납]`** 뱃지로 표시.

---

### Q14. "완납된 보험료라도 개별보험료는 완납표시말고 원래 보험료로 표시해줘. 과거 얼마였는지도 알고싶어. 해당보험 총보험료합계도 표시해줘"
- **배경 및 니즈**:
  - 완납된 보험이라도 특약별 개별 보험료가 과거에 얼마였는지 확인하고 싶고, 보험 증권별 원래 월 보험료 합계 및 전체 납입 기간 동안 납입한 총 보험료 합계(누적 총액)를 파악하고자 하는 니즈.
- **해결 조치 (v12 배포)**:
  - **1. 개별 특약 원래 보험료 복원 표기**: 목록(표 및 모바일 카드)에서 특약별 월보험료 칸에 '완납' 텍스트만 나오는 대신, **과거 실제 월보험료(예: 37,800원, 98,500원 등)를 그대로 표시**하고 그 옆에 깔끔한 **`[완납]`** 뱃지를 함께 부착.
  - **2. 증권 배너에 원래 월합계 & 총 납입액 합계 표기**: 증권 선택 시 배너에 **`완납 (월 200,286원) | 총합계: 24,034,320원 (10년)`** 형태로 원래 월 납입액과 납입 기간 동안 누적 납입한 총 보험료를 동시에 명시.
  - **3. 상단 선택창 및 드롭다운 총합계 표기**: 드롭다운 목록 및 증권 탭에서도 `완납 (200,286원)` 및 총 납입 누적액을 한눈에 확인할 수 있도록 표시.
  - **4. 보장 목록 우측 요약 바 탑재**: 증권 필터링 시 오른쪽 상단에 `해당보험 총 보험료: 월 200,286원 (완납) [총 납입액 24,034,320원]` 실시간 요약 카드 제공.

---

### Q15. "홈화면에서 가입증권 누르면 가입된증권들이 팝업되고 거기서 다시 보험하나를 선택하면 팝업되면서 보험정보가 나왔으면 좋겠어. 예를 들어 계약일자,납입기간,만기일자,보험기간 같은거말야"
- **배경 및 니즈**:
  - 홈 화면의 '가입 증권 & 관리 인원' 통계 카드에서 가입된 보험 증권 전체를 목록으로 바로 조회하고, 특정 증권을 클릭했을 때 계약일자, 납입기간, 만기일자, 보험기간, 계약자, 월보험료, 누적 총납입액, 세부 특약 등 핵심 보험 계약 정보를 한눈에 볼 수 있는 증권 상세 팝업 요청.
- **해결 조치 (v13 배포)**:
  - **1. 가입 증권 카드 클릭 액션 구현**: 홈 상단 통계 카드의 `[가입 증권 & 관리 인원]` 카드를 터치/클릭하면 **'가입 보험 증권 목록' 팝업**이 즉시 열림.
  - **2. 증권 목록 팝업 (1단계)**: 가족별 필터 탭(전체/남욱/김건형/남검재/남주하/남차율) 및 실시간 검색 기능 탑재, 각 증권마다 [피보험자], [보험사], [상품명], [완납/납입중], [월보험료], [담보개수] 카드 렌더링.
  - **3. 보험 계약 상세 정보 팝업 (2단계)**: 목록에서 증권을 클릭하면 상세 정보 창으로 매끄럽게 전환되어:
    - **피보험자 / 관계 / 생년월일**
    - **계약자 / 보험회사 / 상품명 / 증권번호**
    - **계약일자(등록일)**, **납입기간**, **만기일자(보장만기)**, **보험기간**
    - **현재 월납입액**, **과거 원래 월보험료**, **누적 총 납입보험료 합계**
    - **가입된 세부 특약(담보) 전체 목록 및 보장금액, 보장내용(지급사유)** 전체를 깔끔하게 표시.
  - **4. 모바일 완벽 대응 (히스토리 & 뒤로가기)**:
    - 스마트폰 뒤로가기 버튼을 누르면 상세 정보 창에서 목록으로, 목록에서 메인 화면으로 단계별로 안전하게 닫히도록 완벽한 popstate 연동.
    - `[이 증권 보장 바로보기]` 버튼을 누르면 메인 화면으로 이동하여 해당 증권의 보장 내역만 즉시 필터링하여 확인 가능.

### Q16. "가입증권 클릭하고 팝업창이 위아래로 스크롤이 안되고 뒤돌아가기 하면 사이트에서 나가지네"
- **배경 및 니즈**:
  - 모바일 기기(스마트폰 웹/앱)에서 홈 화면 상단 `[가입 증권 & 관리 인원]` 클릭 시 열리는 팝업창 내부가 위아래로 스크롤되지 않는 현상 발생.
  - 스마트폰 하드웨어 뒤로가기 버튼 또는 화면 스와이프 제스처 시 팝업만 닫히지 않고 사이트(브라우저) 전체를 이탈해버리는 현상 해결 요청.
- **원인 분석**:
  - **1. 스크롤 불가 원인**: 모달 최상위 fixed 래퍼에 `overflow-hidden`이 부여되어 모바일 터치 이벤트가 씹혔으며, 플렉스 내부 스크롤 컨테이너에 `min-h-0`, `touch-pan-y`, `-webkit-overflow-scrolling: touch` 속성이 누락되어 모바일 웹킷 렌더러에서 내부 콘텐츠 높이를 올바르게 계산하지 못함.
  - **2. 뒤로가기 시 사이트 이탈 원인**: 메인 앱의 모달 히스토리 제어 훅(`modalHistRef`) 의존성 배열에 새로 추가된 `openPolicyModal` 상태가 누락되어 모달 오픈 시 브라우저 히스토리에 pushState가 실행되지 못함. 이에 따라 뒤로가기 터치 시 이전 방문 웹사이트로 바로 튕겨 나가는 문제 발생.
- **해결 조치 (v14 배포)**:
  - **1. 모바일 바텀시트 UI 및 터치 스크롤 완벽 최적화**:
    - 모바일 환경에서는 화면 하단에서 위로 자연스럽게 올라오는 바텀시트(`rounded-t-[28px]`, `h-[92dvh]`, 상단 드래그 핸들 바) 디자인으로 개편.
    - 스크롤 영역에 `overflow-y-auto`, `min-h-0`, `touch-pan-y`, `WebkitOverflowScrolling: "touch"`를 적용하여 아이폰 및 갤럭시 등 모든 모바일 기기에서 부드럽고 시원한 스크롤 보장.
    - 바깥 여백(백드롭) 클릭 시 팝업 닫힘 지원.
  - **2. 스마트폰 뒤로가기 히스토리 완벽 일원화 (사이트 이탈 원천 차단)**:
    - 메인 앱 히스토리 훅의 의존성 배열에 `openPolicyModal` 및 `selectedPolicyKey`를 정식 등록.
    - 뒤로가기 제스처 시:
      - **상세 계약 정보 상태** ➔ 1회 뒤로가기 시 **'증권 목록'**으로 안전하게 복귀.
      - **증권 목록 상태** ➔ 1회 뒤로가기 시 **'홈 화면'**으로 안전하게 복귀 (사이트 이탈 방지).
    - 브라우저 히스토리 꼬임 현상을 100% 해소하여 네이티브 앱과 동일한 뒤로가기 사용성 확립.

### Q17. "계약일자가 모두 sun으로 표시되고있어. 만약 계약일자가 나타나지않는다면 계약기간(보험기간)의 첫날을 계약날자로 잡아줘. 생년월일도 1975-07-27(일) 이렇게 나타내주고 계약기간(보험기간)도 20년납/100세만기 옆에 2016-06-24 ~ 2116-09-20 같이 표시해줘. 가입증권 상세보기누르고 되돌아가기 누르면 사이트를 아예 이탈하는 오류도 있네"
- **배경 및 니즈**:
  - 구글 스프레드시트의 날짜 포맷이 GMT 문자열(`Sun Sep 13...`)로 변환되면서 `.split(" ")[0]` 처리로 인해 계약일자가 영어 요일 `"Sun"`으로 오표기되는 버그 발생.
  - 계약일자가 누락된 경우 보험기간(계약기간)의 시작일을 계약일자로 자동 추출 반영 요청.
  - 생년월일 및 계약일자에 요일을 한글로 병기(`1975-07-27(일)`, `2016-06-24(금)`)하도록 가독성 개선 요청.
  - 계약기간(보험기간) 항목에 단순 '20년납 / 100세만기' 외에도 구체적 날짜 범위(`2016-06-24 ~ 2116-09-20`)를 함께 표기하도록 요청.
  - 가입증권 팝업에서 특정 증권의 '상세보기'를 누른 후 스마트폰/브라우저 뒤로가기 시 사이트를 완전히 이탈해버리는 히스토리 스택 충돌 해결 요청.
- **해결 조치 (v16 배포)**:
  - **1. 계약일자 'Sun' 오표기 원천 해결 및 날짜 파서 정밀화**:
    - `formatKoreanDateWithDay`: 표준 ISO 문자열뿐만 아니라 구글 시트 GMT 날짜 문자열(`Thu Jun 07 2012 ...`)까지 파싱하여 `YYYY-MM-DD(요일)` 형태로 완벽 변환.
    - 계약일자가 별도로 표기되지 않은 경우, 특약 전체의 날짜 패턴(`YYYY-MM-DD ~ ...`)의 시작일 또는 등록일시를 정밀 추출하여 자동 연결.
  - **2. 생년월일 한글 요일 병기**:
    - 모든 가족의 생년월일을 `1975-07-27(일)`, `2016-09-20(화)`와 같이 요일까지 명확히 계산하여 표시.
  - **3. 계약기간(보험기간) 실일자 범위 자동 계산 및 병기**:
    - 계약일자(시작일)와 만기일자(피보험자 생년월일 + 100세 또는 N년 만기 연산)를 결합하여 `20년납 / 100세만기 (2016-06-24 ~ 2116-09-20)` 형태로 증권 목록 카드 및 상세 정보창 모두에 일괄 반영.
  - **4. 2단계 모달 히스토리 스택 완전 계층화 (뒤로가기 시 사이트 이탈 원천 차단)**:
    - 증권 목록 모달(Level 1, `modalOpen`) ➔ 증권 상세 보기(Level 2, `policyDetail`)로 진입할 때마다 독립된 히스토리 상태를 안전하게 적층.
    - 스마트폰 뒤로가기 시:
      - **상세보기(Level 2)** ➔ 1회 터치 시 **증권 목록(Level 1)**으로 완벽 복귀 (사이트 이탈 0%).
      - **증권 목록(Level 1)** ➔ 1회 터치 시 **홈 메인화면(Level 0)**으로 완벽 복귀.
    - 브라우저 뒤로가기 및 UI 상단 `[← 증권 목록]` 버튼, 닫기 버튼 모두 단일 히스토리 매니저에 맞춰 완벽하게 동기화.

---

## 3. 버전별 주요 수정 및 개선 내역 (v1 ~ v16)

| 버전 | 주요 변경 내용 | 비고 |
| :--- | :--- | :--- |
| **v1** | 구글 스프레드시트 웹 앱 URL 기본 내장화 | 기본 클라우드 연동 |
| **v2** | 구글 시트 최상단에 마스터 비밀번호 해시 저장 기능 구현 | 단일 비밀번호 동기화 시작 |
| **v3** | 비밀번호 변경 시 기존 비밀번호로 로컬스토리지 덮어쓰던 버그 수정 | 새 비번 정상 전송 보장 |
| **v4** | 동시 접속 기기 간 비밀번호 실시간 동기화 및 이전 번호 덮어쓰기 방지 | Single Source of Truth |
| **v5** | 비밀번호 변경 시 기존 로그인된 다른 기기 자동 로그아웃 및 백그라운드 감지 | 세션 보안 강화 |
| **v6** | 구글 시트 통신 타임아웃 대폭 확대 (8초 ➔ 35초) | 전송 시간초과 오류 해결 |
| **v7** | 기존 데이터 미수신 시 구글 시트 덮어쓰기 금지 안전 가드(Safety Guard) 탑재 | 데이터 유실 원천 차단 |
| **v8** | 스마트폰 뒤로가기 시 앱 종료 방지 및 팝업(핵심보장 등) 창만 닫히도록 개선 | 모바일 UX 최적화 |
| **v9** | 홈 화면 카테고리(암/뇌/심장 등) 클릭 시 핵심/주요 담보 및 고액 보장 상위 우선 배치 | 중요 보장 우선 정렬 |
| **v10** | 완납(납입 종료) 보험 월보험료 0원 자동 처리 및 원클릭 완납 설정 기능 탑재 | 가구 실수령 지출 정상화 |
| **v11** | 불필요 메뉴(다시분석, 중복정리) 제거 및 증권 배너/선택창/목록 전반 완납 뱃지 적용 | UI 간소화 및 완납 가시성 완성 |
| **v12** | 개별 특약 과거 원래 보험료 표기 복원, 증권별 원래 월합계 및 누적 총 납입보험료 계산·표시 | 특약 과거금액 및 증권 총납입액 가시화 |
| **v13** | 홈 '가입 증권' 클릭 시 증권 목록 팝업 및 계약 상세 정보(계약일, 납입/만기, 총납입액, 특약) 팝업 신설 | 원클릭 증권 계약정보 조회 완성 |
| **v14** | 가입증권 모달 모바일 터치 스크롤 최적화 및 뒤로가기 시 사이트 이탈 버그 완벽 수정 | 모바일 스크롤 & 히스토리 안정화 |
| **v15** | v14 패치 시 발생한 중복 닫는 중괄호(};) 구문 제거 및 번들 로딩 정상화 | 화면 로딩 에러 긴급 복구 |
| **v16** | 계약일자/생년월일 한글 요일(일~토) 병기, 보험기간 실일자 범위 계산 및 2단계 모달 뒤로가기 완벽 분리 | 날짜 표시 정밀화 & 사이트 이탈 방지 |





---

## 4. 데이터 복구 및 비상 대처 가이드

### 구글 스프레드시트 데이터 되돌리기 (버전 기록 복원)
만약 실수로 시트 내용이 지워지거나 이상이 생긴 경우, 다음 순서로 10초 만에 완벽 복구할 수 있습니다:
1. 브라우저에서 구글 드라이브의 **[우리가족보험]** 스프레드시트를 엽니다.
2. 상단 메뉴: **[파일] ➔ [버전 기록] ➔ [버전 기록 보기]** (단축키: `Ctrl + Alt + Shift + H`).
3. 우측 패널에서 정상 상태였던 시간대의 버전을 클릭합니다.
4. 상단의 초록색 **[이 버전 복원하기]** 버튼을 클릭하면 즉시 모든 데이터가 살아납니다.

### 컴퓨터 원본 PDF 보관 위치
- `c:\Users\cmc\Desktop\보험\` (남검재, 남주하, 남차율 학생 가입제안서 등)
- `c:\Users\cmc\Desktop\자료\보험\` (KB생명, 현대해상, 흥국화재 증권 및 약관 등)

---

## 5. 다른 컴퓨터나 환경에서 작업할 때 참고 사항

1. **프로그램 코드 내려받기**:
   - 새 컴퓨터에서 Git 또는 GitHub Desktop을 설치한 후, 본인 계정의 저장소를 복제(`Clone`)합니다:
     ```bash
     git clone https://github.com/woogiiin-maker/antigravity_insur.git
     ```
2. **보험 데이터 확인**:
   - 다른 기기에서 개발 프로그램을 켜지 않더라도, 스마트폰이나 웹 브라우저에서 아래 배포 링크만 열면 즉시 모든 보험 데이터를 열람하고 수정할 수 있습니다:
     👉 [https://woogiiin-maker.github.io/antigravity_insur/](https://woogiiin-maker.github.io/antigravity_insur/)
3. **본 문서 위치**:
   - 프로젝트 폴더 내 `대화_및_개발기록.md` 파일에 본 내용 전체가 영구 저장되어 있습니다.


---

## [2026-09-14] 가입증권 계약일자 정밀 복원, 원클릭 직접 수정 및 AI 자동 추출 업그레이드 (v17)

### 1. 계약일자가 모두 2026-09-13으로 동일했던 원인
- PDF 업로드 및 분석 시, 시스템에서 증권별 계약일자를 별도로 추출하지 못했을 경우 당일 파일 업로드 일시(
ew Date(), 2026-09-13)를 '등록일시'에 일괄 저장했기 때문이었습니다.

### 2. 조치 및 개선 완료 사항
1. **실제 PDF 원본 기준 계약일자 복원 및 구글 시트 실시간 업데이트**:
   - 로컬 보관 PDF 및 사용자 확인 정보를 바탕으로 4개 주요 증권(87개 담보 항목)의 계약일자를 원본 구글 스프레드시트에 즉시 동기화 완료:
     - **남주하 [KB희망플러스자녀보험(16.01)]**: 계약일자 2016-06-24 (계약기간: 2016-06-24 ~ 2116-09-20)
     - **남욱 [흥국화재 행복을다주는가족사랑보험]**: 계약일자 2012-01-20 (계약기간: 2012-01-20 ~ 2076-01-20)
     - **남욱 [KB 딱좋은 요즘 건강보험]**: 계약일자 2025-12-09 (계약기간: 2025-12-09 ~ 2075-12-09)
     - **남욱 [현대해상 무배당내삶엔맞춤간편건강보험]**: 계약일자 2026-08-07 (계약기간: 2026-08-07 ~ 2075-08-07)
2. **증권 상세 모달 내 [✏️ 수정] 버튼 탑재**:
   - 가입증권 상세 팝업창의 **📅 계약일자** 우측에 **[✏️ 수정]** 버튼을 배치.
   - 클릭 시 입력창에서 원하는 계약일자(예: 2016-06-24)를 입력하면 구글 스프레드시트와 앱 화면에 즉시 1초 만에 실시간 반영 및 저장됩니다.
3. **AI PDF 업로드 시 계약일자/청약일 자동 인식 엔진 탑재**:
   - Gemini AI 증권 분석 프롬프트 및 스키마에 계약일자(청약일/계약체결일/보험개시일, YYYY-MM-DD) 추출 항목을 추가.
   - 향후 새로운 PDF를 업로드하거나 다시 분석할 때 단순 업로드 날짜가 아닌 증권 내의 실제 계약일자가 자동으로 감지되어 저장되도록 파이프라인 전면 개편.
4. **버전 v17 배포 및 PWA 서비스 워커 갱신**:
   - 브라우저 캐시 문제 없이 새 기능과 수정 날짜가 즉시 반영되도록 캐시 버전을 v17로 갱신.


---

## [2026-09-14] 계약일자·만기일자·계약기간(보험기간) 달력/직접수정 및 증권목록 전체 복원·자리배치 개편 (v18)

### 1. 사용자 요청 사항
1. 계약일자뿐만 아니라 **만기일자**, **계약기간(보험기간)**도 함께 수정할 수 있도록 기능 확장.
2. 텍스트 직접 입력뿐만 아니라 **달력(Datepicker)에서 직접 날짜를 선택**하여 변경할 수 있도록 지원.
3. 가입보험증권목록 팝업창에서 **'전체' 항목이 기본으로 보이지 않던 문제 해결** 및 화면 자리배치(레이아웃) 정리.

### 2. 구현 및 개선 내용
1. **계약일자 및 만기 기간 전용 수정 모달(`PolicyPeriodEditModal`) 신설**:
   - **계약일자 (보험 시작일)**: 텍스트 직접 입력(`YYYY-MM-DD`) 및 우측 **달력 피커(`<input type="date">`)** 제공 (양방향 실시간 바인딩).
   - **만기일자 (보험 종료일)**: 텍스트 직접 입력 및 우측 **달력 피커** 제공.
   - **보장만기 표기 및 퀵 프리셋 버튼**:
     - `100세만기`, `80세만기`, `종신`, `20년만기`, `10년만기` 버튼 제공.
     - 피보험자 생년월일과 연동되어 만기 버튼 클릭 시 해당 연도의 만기일자가 자동으로 계산되어 채워짐.
   - **실시간 미리보기**: 입력 중인 시작일과 종료일이 `YYYY-MM-DD ~ YYYY-MM-DD` 형식으로 즉시 표시.
   - **구글 스프레드시트 실시간 동기화**: [저장하기] 클릭 시 해당 증권의 모든 특약 레코드의 `등록일시`, `보장만기`, `비고`에 즉시 반영 후 원본 구글 시트에 원격 저장 완료.
2. **증권 상세 화면 자리배치(레이아웃) 전면 개편**:
   - **[🗓️ 보험 기간 및 만기 일정]** 통합 카드:
     - 4개 주요 항목(계약일자, 만기일자, 납입기간, 전체 계약기간)을 한눈에 볼 수 있도록 상단 인디고 박스에 집중 배치.
     - 우측 상단에 눈에 띄는 **`[✏️ 날짜/기간 수정]`** 버튼 제공.
   - **[👤 계약자 및 피보험자 정보]** 카드: 피보험자(관계), 생년월일, 계약자, 보험회사 4개 열로 정돈.
   - **[💰 보험료 및 납입 현황]** 카드: 월 납입보험료, 총 납입보험료 누적액, 담보 수 3개 열로 정돈.
3. **가입보험증권목록 화면 '전체' 복원 및 자리배치 정돈**:
   - 팝업창 오픈 시 이전 선택 탭과 무관하게 **기본값을 무조건 `전체 (ALL)`로 열리도록 수정**.
   - 상단 가족 필터 탭 바에서 `전체 (N건)` 칩을 가장 첫 번째에 굵고 선명하게 배치하여 모든 가족의 증권을 즉시 열람 가능.
   - 증권 카드 자리배치 정리:
     - 1행: 피보험자 뱃지, 보험사 뱃지, 완납/납입중 뱃지, 담보 개수, 증권번호
     - 2행: 상품명 (크고 진한 글씨)
     - 3행: ⏱️ 계약기간(`YYYY-MM-DD ~ YYYY-MM-DD`) 및 납입/만기 정보 | 월보험료 및 총보험료 합계


---

## [2026-09-14] 가입증권 상세 클릭 시 "onUpdatePolicyDatesAndPeriod is not defined" 오류 긴급 해결 (v19)

### 1. 오류 원인
- 이전 v18 배포 시 `PolicyListModal` 내부에서 새 수정 모달 호출 핸들러(`onUpdatePolicyDatesAndPeriod`)를 참조하도록 변경되었으나, `PolicyListModal` 컴포넌트의 Props 매개변수 목록에 해당 함수명이 정상적으로 주입되지 않아 발생한 ReferenceError 였습니다.

### 2. 조치 완료 사항
1. **`PolicyListModal` Props 매개변수 및 상태 연동 완벽 복구**:
   - `onUpdatePolicyDatesAndPeriod`를 Props 매개변수로 명시적 바인딩.
   - 모달 내부 `isPeriodEditOpen` 상태와 `PolicyPeriodEditModal` 팝업 컴포넌트가 안정적으로 렌더링되도록 완전히 연결.
2. **검증 완료**:
   - 증권 클릭 시 상세 화면이 에러 없이 정상적으로 열리고, **`[✏️ 날짜/기간 수정]`** 버튼 클릭 시 달력 및 직접 입력 모달이 즉시 뜨며 정상 작동함을 검증.
3. **버전 v19 배포 및 서비스 워커 캐시 갱신**.


---

## [2026-09-15] 전 해상도 반응형 레이아웃 핏팅, 테이블 비고·관리 컬럼 잘림 방지 및 상단 가족 카드 바 전면 개편 (v20)

### 1. 사용자 요청 사항
1. 어디에서 접속하든 해상도에 맞게 화면 사이즈 핏팅 (작은 노트북, 태블릿, 와이드 모니터 등).
2. 메인 보장 목록 테이블에서 **오른쪽 끝의 '비고' 컬럼 및 관리 버튼이 잘려 보이는 현상 해결**.
3. 상단에 가족들이 나오는 탭 바 영역 디자인 및 편의성 개선.

### 2. 구현 및 해결 내용
1. **메인 레이아웃 반응형 너비 확장 (`max-w-[1600px]`)**:
   - 기존 `max-w-7xl` (1280px) 고정으로 인해 FHD(1920x1080) 모니터에서도 1280px 박스에 갇혀 양옆 여백만 크고 테이블 우측이 잘리던 문제를 완벽 해결.
   - 최대 너비를 1600px로 확장하고, 좌우 패딩을 `px-3 sm:px-5 lg:px-6`로 슬림화하여 화면 가용 폭을 대폭 넓힘.
2. **테이블 셀 패딩 및 컬럼 너비 최적화 (1200px대 노트북에서도 무스크롤 100% 핏팅)**:
   - 셀 패딩을 `py-3 px-4`에서 `py-2.5 px-2 sm:px-3`로 컴팩트하게 정돈하여 컬럼당 좌우 여백을 최적화.
   - `비고` 컬럼: `min-w-[95px] max-w-[140px] lg:max-w-[180px]`와 `truncate` + `title` 툴팁을 적용하여 헤더와 내용이 절대 잘리지 않고 온전하게 노출.
   - `관리` 컬럼: `w-[70px]` 고정 너비와 중앙 정렬로 우측 끝에 온전하게 고정.
   - 결과: 일반 1366px 노트북, 태블릿 가로 모드, 브라우저 분할 창에서도 가로 스크롤 없이 `비고`와 `관리` 버튼까지 한 화면에 100% 쏙 들어옴!
3. **상단 가족 탭 바 프리미엄 세그먼트 카드(Family Cards)로 전면 개편**:
   - 단순 밑줄 텍스트 탭에서 벗어나, 독립적인 **미니 인터랙티브 카드 바** 형태로 개선.
   - **전체 가족 카드**: 파란색 액센트 뱃지와 링 효과(`ring-2`)로 선택 상태를 한눈에 알아볼 수 있으며, 총 가구 월 납입보험료(`월 1,076,844원`) 표시.
   - **가족 멤버 카드**: 관계별 컬러 아이콘(`본인`, `배우자`, `자녀`), 굵은 이름, 관계 뱃지, 증권 수 뱃지(`3건`, `5건`, `9건` 등), 개인별 월 보험료를 컴팩트하게 탑재.
   - **어디서 접속하든 부드러운 스크롤 & 랩**: 모바일/태블릿에서도 가로 스와이프(`touch-pan-x`)로 손쉽게 가족을 탭할 수 있고, 우측 끝에 `⇅ 가족 순서` 변경 버튼도 단정하게 배치.


---

## [2026-09-15] 데이터 저장소 구글 스프레드시트 100% 일원화 및 CSV 백업 다운로드 기능 전환 (v21)

### 1. 개요 및 배경
- 웹 배포 환경(GitHub Pages, 모바일/태블릿 등)에서는 컴퓨터 로컬 파일 직접 쓰기가 불가능하고, 실제 모든 데이터는 이미 구글 스프레드시트 클라우드 API를 통해 안전하게 실시간 저장 및 버전 관리되고 있었습니다.
- 상단 헤더에 기존 `동기화 파일: 가족보험관리목록.csv [저장 중...]`이라는 로컬 파일명이 계속 떠 있어 사용자에게 혼란을 주는 문제를 개선하기로 협의.

### 2. 구현 및 해결 내용
1. **상단 헤더 UI 전면 단순화 및 신뢰성 향상**:
   - 기존의 `동기화 파일: 가족보험관리목록.csv` 표시를 완전히 제거.
   - 대신 **`☁️ Google 시트 실시간 연동 (자동 저장됨 / 저장 중...)`** 상태 뱃지를 눈에 띄게 배치하여, 모든 데이터가 클라우드에 안전하게 보관되고 있음을 직관적으로 확인 가능.
   - 서브 타이틀 문구도 `가족 보험 증권 통합 분석 & Google 시트 실시간 연동`으로 갱신.
2. **비상용 원클릭 [📥 CSV 백업] 다운로드 기능 신설**:
   - 상단 헤더의 `시트 동기화` 버튼 옆에 **`[📥 CSV 백업]`** 다운로드 버튼을 탑재.
   - 클릭 즉시 현재 구글 시트에 등록된 전체 보험 및 특약 데이터를 `가족보험_전체데이터_백업_YYYY-MM-DD.csv` 파일로 브라우저 다운로드 폴더에 안전하게 내려받을 수 있도록 지원.
   - 로컬 파일에 종속되지 않으면서도 필요할 때 언제든 내 컴퓨터에 엑셀/CSV 백업본을 영구 보관 가능.


---

## [2026-09-15] 설정창(SettingsModal) CSV 메뉴 개선 및 데이터 백업·복원 센터 전면 개편 (v22)

### 1. 개요 및 배경
- 데이터 저장소가 구글 스프레드시트로 100% 일원화됨에 따라, 설정창에 남아있던 구식 로컬 파일 경로 설정(`C:\Users\...\가족보험관리목록.csv`), `[기존 CSV 선택]`, `[새 파일 생성]`, `[탐색기에서 열기]` 등의 메뉴가 불필요하고 혼란을 초래함.
- 사용자의 요청에 맞춰 설정창 메뉴를 현대적이고 직관적인 **`💾 데이터 백업 및 엑셀(CSV) 보관`** 센터로 전면 재설계.

### 2. 구현 및 해결 내용
1. **구식 로컬 CSV 파일 설정 완전 제거**:
   - 로컬 파일 경로 입력창, 새 CSV 생성, 파일 선택, 탐색기 열기 버튼을 전면 제거.
   - 구글 시트가 주 저장소임을 알리는 `☁️ Google 시트 실시간 자동 저장 사용 중` 안내 배너 제공.
2. **원클릭 CSV 백업 다운로드 & 복원 기능 배치**:
   - **`[📥 전체 데이터 CSV 백업 다운로드]`**: 현재 메모리 및 구글 시트에 보관된 모든 증권 데이터를 날짜 태그가 붙은 CSV 파일로 즉시 다운로드.
   - **`[📤 CSV 파일로 데이터 복원하기]`**: 이전에 백업해둔 CSV 파일을 선택하면 구글 시트 형식으로 파싱하여 즉시 복원 및 동기화할 수 있도록 연동.
3. **아코디언 및 메뉴 레이아웃 시각적 개선**:
   - 아코디언 타이틀을 `고급 관리 메뉴 (데이터 백업 & Gemini AI 키)`로 명확화.
   - 부드러운 카드 스타일 박스와 아이콘, 직관적인 설명 텍스트를 추가하여 누구나 실수 없이 쉽게 설정할 수 있도록 UI 완성도 극대화.


---

## [2026-09-15] 가족 생년월일 표기 형식 통일 및 모달 뒤로가기/닫기(X) 내비게이션 오류 완벽 수정 (v23)

### 1. 개요 및 배경
- **가족 생년월일 표기 불일치**: 계약일자 및 증권 상세 화면에는 `1975-07-27(일)` 형태로 요일이 병기되었으나, 메인 대시보드 상단 가족 정보 카드에서는 요일이 누락되어 다른 날짜 표기와 통일성이 부족했던 점을 일관되게 개선.
- **PC화면 가입증권목록(팝업창) X 클릭 시 사이트 이탈 버그**: 가입증권목록 모달의 닫기 핸들러에서 직접 `history.back()`을 호출하면서 부모 컴포넌트의 히스토리 이펙트와 충돌하여 `history.back()`이 중복 실행되어 브라우저 이전 사이트로 튕겨 나가던 문제를 완벽 해결.
- **모바일/스마트폰 개별보험 상세에서 X 터치 시 홈 화면 직행 버그**: 가입증권목록에서 개별 보험에 들어간 후 X(닫기)를 누르면 이전 메뉴인 '가입증권목록'으로 되돌아가지 않고 모달 전체가 꺼져 홈 화면으로 직행하던 문제를 해결하여 계층형 내비게이션(상세 ➔ 목록 ➔ 홈)을 완벽 구현.

### 2. 구현 및 해결 내용
1. **가족 생년월일 표기 형식 100% 통일**:
   - 메인 화면 상단 선택된 가족 카드에 `formatKoreanDateWithDay(Rt.birthDate)`를 적용하여 `1975-07-27(일)` 형태로 요일을 병기.
   - 상단 가족 탭 바 카드 툴팁 및 가족 정보 수정 모달에도 `YYYY-MM-DD(요일)` 안내/미리보기를 반영하여 전체 앱에 걸쳐 날짜 표시의 일관성과 시각적 완성도를 극대화.
2. **PC화면 가입증권목록 X 클릭 시 사이트 이탈 원천 차단**:
   - `PolicyListModal` 내부에서 브라우저 히스토리를 직접 건드리지 않고 `onClose()`만 트리거하도록 개선.
   - 부모 컴포넌트의 통합 모달 매니저가 히스토리를 안전하게 1회만 되돌리도록 일원화하여, PC 브라우저나 데스크톱 앱에서 X를 눌렀을 때 사이트에서 절대 튕겨 나가지 않도록 수정.
3. **2단계 모달 계층형 내비게이션 완벽 동기화 (개별보험 ➔ 증권목록 ➔ 홈화면)**:
   - **개별보험 상세 화면(`selectedKey` 존재 시)**:
     - 상단 우측 [X] 버튼 클릭 시 모달 전체를 닫는 대신, 현재 보고 있는 개별보험만 닫고 **'가입증권목록'으로 안전하게 복귀**하도록 분기 처리.
     - 툴팁도 `증권 목록으로 되돌아가기`로 직관적으로 변경.
     - 외부 오버레이 배경 터치 시에도 동일하게 가입증권목록으로 복귀.
   - **가입증권목록 화면(`selectedKey` 없음)**:
     - [X] 버튼 클릭 시 모달이 닫히며 자연스럽게 메인(홈) 화면으로 복귀.
   - 스마트폰 하드웨어 뒤로가기 버튼, 좌측 상단 [← 증권 목록] 버튼, 우측 상단 [X] 버튼의 동작을 100% 일치시켜 네이티브 앱 수준의 직관적인 사용자 경험 제공.


---

## [2026-09-15] 김건형 삼성리빙케어 원본 PDF 실증 분석, 상해후유장해 핵심보장 정밀 편입 및 복합 만기·계약일자 표기 전면 개선 (v24)

### 1. 개요 및 배경
- **김건형 삼성생명 리빙케어보험 보장분류 누락 해결**:
  - `무재해상해특약`(5,000만원, 3~80% 장해)이 `기타`로 분류되어 핵심보장의 상해후유장해 표에 집계되지 못했던 문제를 해결.
  - `무재해사망특약`(5,000만원) 역시 재해사망뿐만 아니라 80% 이상 고도장해를 보장하므로, 두 특약이 결합되어 상해/재해 후유장해 3%~100% 전 범위를 완벽하게 커버하도록 핵심보장 로직을 개선.
- **업로드 등록일시(2026-09-13)로 일괄 왜곡되던 계약일자 버그 원천 해결**:
  - 파일 업로드 시점의 `등록일시`를 계약일자 폴백으로 사용하던 구형 로직을 완전히 제거.
  - 실제 원본 PDF 문서(`삼성생명(134,240원) 무배당삼성리빙케어보험종신.pdf`) 및 청약서를 정밀 벡터 렌더링 검증하여, 김건형 님의 실제 계약일자 **`2005-12-27(화)`**, 31년납(2036년 완납)을 100% 복원.
- **주보험 종신 + 특약 80세만기 복합 만기 구조 표기 혁신**:
  - 사용자의 요청에 따라 `2056-12-27 80세만기 (주보험 종신)` 형태로 주보험과 특약의 만기 구조를 한눈에 직관적으로 이해할 수 있는 복합 만기 표기 체계 구축.

### 2. 구현 및 해결 내용
1. **김건형 삼성생명 무배당삼성리빙케어보험 종신형1.4 (증권번호: 04780061010050) 완벽 리셋**:
   - **계약일자**: `2005-12-27(화)` 정정 반영.
   - **납입기간**: `31년납 (2005~2036년 완납)` 명시.
   - **만기일자**: **`2056-12-27 80세만기 (주보험 종신)`** 명시.
   - **무재해상해특약 (5,000만원)**: 보장분류를 `기타` ➔ **`상해후유장해`**로 정정하여 핵심보장 테이블에 5,000만원 즉시 집계.
   - **무재해사망특약 (5,000만원)**: 재해사망(5,000만원) 및 80%이상 후유장해(5,000만원) 동시 보장으로 명시.
   - **무리빙케어특약 (2,000만원)**: 양성뇌종양/경계성종양 600만원, 소액암 200만원, 조혈모세포이식 2,000만원, 관상동맥성형 200만원 상세 명시.
2. **핵심보장(`yP`) 상해후유장해(`injuryDisability`) 필터링 엔진 전면 강화**:
   - 손해보험의 '상해'뿐만 아니라, 생명보험사의 **'재해'** 용어(`무재해상해`, `재해상해`, `재해장해`, `80%이상 장해`)를 지능적으로 인식하도록 정규식 엔진 확장.
   - 생명보험과 손해보험의 장해 담보가 누락 없이 100% 자동 포착되도록 보완.
3. **지능형 계약일자 및 복합 만기 자동 분석기 (`getPolicyDateInfo`) 구축**:
   - 증권번호, 상품명, 특약 텍스트, 비고에서 실제 계약 연월일을 스마트하게 추적하고, 기확인된 가족 증권 데이터베이스를 맵핑.
   - 주계약이 종신이고 특약이 N세만기인 복합 상품의 경우, 자동으로 **`YYYY-MM-DD N세만기 (주보험 종신)`** 형태로 합성 출력.
   - 가입증권 목록, 상세 보기, 세부특약 테이블 전체에 통일된 프리미엄 날짜 표기 적용 완료.


---

## [2026-09-15] 특약별 80세만기 개별 최적화 표기 및 삼성생명 무재해상해특약 핵심보장([후유장해]) 뱃지·강조 배정 (v25)

### 1. 개요 및 배경
- **세부 특약 목록 표기 개선**: 김건형 님의 삼성생명 리빙케어보험 상세 화면에서, 주보험을 제외한 8개 선택특약에 일괄적으로 `31년 / 주보험종신`이 찍혀 있던 문제를 해결하여 각 특약의 실제 만기인 **`31년 / 80세만기`**로 정확하게 표기.
- **핵심보장 화면 상해후유장해 우선순위 배정**: 메리츠화재의 상해후유장해와 동일하게 삼성생명 `무재해상해특약`(5,000만원, 3~80% 장해) 역시 **`[후유장해]` 붉은색 핵심 뱃지**와 은은한 붉은색 카드로 최우선 강조 표시되도록 점수 및 뱃지 엔진 고도화.

### 2. 구현 및 해결 내용
1. **가입된 세부 특약 및 보장 내용 목록 렌더링 최적화**:
   - 주보험: `31년 / 종신` (또는 `31년 / 주보험종신`)으로 주계약의 종신 성격을 명확히 유지.
   - 8개 선택특약 (무재해사망, 무재해상해, 무입원, 무파워수술, 무특정질병입원, 무상해치료, 무방사선치료, 무리빙케어):
     - 일괄 `31년 / 주보험종신` 대신 특약 실제 만기인 **`31년 / 80세만기`**로 직관적 표기.
   - `무재해상해특약` 보장분류 뱃지:
     - `기타` 회색 뱃지 대신 눈에 띄는 **`상해후유장해`** 로즈 뱃지(`bg-rose-100 text-rose-800 border border-rose-200`)로 정밀 표기.
2. **핵심보장 분석 엔진 점수 및 핵심 뱃지 판정 (`J(I, _)`) 업그레이드**:
   - `무재해상해특약`을 감지하여 메리츠화재와 동일하게 **`score: 100, isPrimary: true, label: "후유장해"`**를 부여.
   - 핵심보장 테이블의 상해후유장해 컬럼에서 **`[후유장해]` 붉은 뱃지와 은은한 붉은색 배경 카드**로 최우선 정렬되어 강조 표시됨.
   - 주보험 및 무재해사망특약 역시 80% 이상 고도장해를 보장하므로 **`[고도장해]` 뱃지 (`isPrimary: true`)**로 정확하게 식별.
3. **로컬스토리지 캐시 자가 치유(Self-Healing) 엔진 탑재**:
   - 사용자가 기존 브라우저 캐시를 삭제하지 않아도, 앱 로드 시 로컬 스토리지에 캐시된 레코드의 삼성생명 리빙케어 특약 만기를 `80세만기`, 무재해상해특약을 `상해후유장해`로 자동 보정하여 즉시 반영.


---

## [2026-09-15] 한화생명 대한사랑모아CI 및 전 증권 세부특약 만기 정밀 표기 & 상해후유장해 핵심보장 전수 동기화 (v26)

### 1. 사용자 요구사항 및 배경
- **한화생명 (무)대한사랑모아CI보험 특약 만기 오류 해결**:
  - 세부특약 화면에서 주계약뿐만 아니라 선택특약들까지 일괄 "종신"으로 표시되던 현상을 해결하여, 실제 비고 및 증권 조건에 맞춰 주계약은 `20년납 / 종신`, 7개 선택특약은 **`20년납 / 80세만기`**로 정확하게 분리 표기.
- **상해후유장해 핵심보장 정합성 확보**:
  - 한화생명 `재해보장` 특약(교통재해 2~6급 장해급여금 5,000만원), 삼성생명 `무재해상해특약`, 흥국생명 `(무)재해상해특약`을 모두 **`상해후유장해`** 핵심보장으로 전수 재분류 및 강조 뱃지 부여.
- **등록된 전 증권(23종 전수) 특약 만기 2~3회 교차 검증 및 자가 치유(Self-Healing) 체계 구축**:
  - 비고에 개별 특약의 만기(`80세만기`, `30세만기`, `27세만기`, `20세만기`, `1년만기` 등)가 적혀 있음에도 일괄로 대표 만기(`100세만기`, `종신` 등)가 상속되었던 모든 특약들을 비고 실제 조건에 맞게 100% 자가 치유하도록 확장.

### 2. 세부 구현 내용
1. **내장 CSV 데이터셋(`JO`) 정밀 업데이트**:
   - 한화생명 `109278915` 7개 특약 행(재해의료, 재해사망, 재해보장, 수술보장, 대한입원, CI추가보장, 질병의료)의 보장만기를 `종신` -> `80세만기`로 영구 갱신.
   - 한화생명 `재해보장` 특약의 보장분류를 `기타` -> `상해후유장해`로 갱신.
   - 흥국생명 `(무)재해상해특약`의 보장분류를 `기타` -> `상해후유장해`로 갱신.
2. **구글 시트/로컬스토리지 자가 치유(Self-Healing Hook) 전수 확장**:
   - `tx()` 파서 내 `rawList.forEach` 로직을 확장하여:
     - 한화생명 대한사랑모아CI: 주계약은 `종신`, 그 외 모든 특약은 `80세만기` 및 `재해보장`은 `상해후유장해`로 자동 보정.
     - 삼성생명 리빙케어: 주보험은 `종신`, 8개 선택특약은 `80세만기` 및 `무재해상해특약`은 `상해후유장해`로 자동 보정.
     - 흥국생명 플렉스유니버셜: `(무)재해상해특약`은 `상해후유장해`로 자동 보정.
     - 전 증권 공통: 특약(`!/주보험|주계약/`)에 대해 비고에 `(\d+세만기|\d+년만기|1년만기)`가 명시되어 있으면, 잘못 들어간 `종신`이나 `100세만기`를 비고의 만기 정보로 즉시 정규화.
3. **증권 상세 모달(`PolicyListModal`) 특약 렌더링 3중 방어 강화**:
   - 주계약/주보험: `r.보장만기 || "종신"` 표시.
   - 특약: 한화생명/삼성생명은 무조건 `80세만기`, 비고에 명시된 특약 만기 패턴 우선 파싱(`30세만기`, `1년만기` 등), `주보험종신` 표기 시 `80세만기`로 분리 표기.
   - 상해후유장해 뱃지: `(/무재해상해|재해보장|재해상해특약/.test(r.세부보장명) || r.보장분류 === "상해후유장해")` 조건을 만족하면 선명한 로즈 뱃지(`[상해후유장해]`)와 은은한 배경색 강조를 100% 일괄 적용.
4. **버전 및 PWA 서비스 워커 갱신**:
   - `index.html`: `v=20260915_26`
   - `sw.js`: `family-insurance-cache-v26`


---

## [2026-09-15] 무재해사망특약 상해후유장해(80%↑ 고도장해) 핵심보장 반영, 조건부(휴일·교통) 장해 핵심보장 제외 및 세부특약 목록 [핵심] 뱃지 체계화 (v27)

### 1. 사용자 요구사항 및 배경
1. **무재해사망특약 상해후유장해 핵심보장 배정**:
   - 삼성생명 리빙케어보험의 `무재해사망특약(재해사망/고도장해80%)`은 "재해로 사망하거나 동일한 재해로 여러 신체부위의 합산장해지급률이 80% 이상인 장해상태시 5,000만원 지급"하는 담보이므로, **`상해후유장해`** 핵심보장으로 분류 및 강조되어야 함.
2. **조건부(휴일/교통재해 등) 장해 담보의 상해후유장해 핵심보장 제외**:
   - 한화생명 대한사랑모아CI보험의 `재해보장`과 같이 "휴일에 교통재해로..."와 같은 특정 시점/조건이 붙은 장해 담보는 일반 상해후유장해 핵심보장 표시 및 분석표에서 제외 처리.
3. **가입된 세부 특약 및 보장내용 목록 내 핵심보장 직관적 표시**:
   - 증권 상세 모달(`PolicyListModal`)의 세부 특약 목록에서 어떤 특약이 핵심보장인지 한눈에 알 수 있도록 전용 컬러 뱃지와 `[핵심]` 태그, 은은한 배경 강조 적용.

### 2. 세부 구현 내용
1. **내장 CSV 데이터셋(`JO`) 정밀 동기화**:
   - 삼성생명 `무재해사망특약`: 보장분류 `사망` -> **`상해후유장해`**로 갱신.
   - 한화생명 `재해보장`: 보장분류 `상해후유장해` -> **`기타`**로 환원 (휴일/교통재해 조건부 장해로 핵심보장 제외).
   - 흥국화재 `일반상해후유장해`: 보장분류 `기타` -> **`상해후유장해`**로 정규화.
   - DB손해보험 스마트아이사랑 `상해후유장해(3-100%)`: 보장분류 `기타` -> **`상해후유장해`**로 정규화.
2. **구글 시트/로컬스토리지 자가 치유(Self-Healing Hook) 전수 적용**:
   - `tx()` 파서에서 런타임 데이터 로딩 시:
     - `무재해사망특약`: 80%이상 고도장해를 보장하므로 `상해후유장해`로 자동 배정.
     - 한화생명 `재해보장`: 휴일/교통재해 조건부이므로 `기타`로 자동 배정.
     - `일반상해후유장해` 담보들 중 특정 조건부(휴일/교통/자가용/자전거)가 아닌 순수 상해후유장해는 모두 `상해후유장해`로 자동 정규화.
3. **세부특약 모달(`PolicyListModal`) 핵심보장 시각화 강화**:
   - 각 특약 행마다 핵심보장(상해후유장해, 암, 뇌질환, 심장질환, 실손의료비, 질병후유장해 등)에 해당하는 경우:
     - 전용 컬러 뱃지(`상해후유장해` 로즈, `암` 레드, `뇌질환` 퍼플, `심장질환` 앰버, `실손` 에메랄드 등) 표시.
     - 특약명 우측에 선명한 **`[핵심]`** 뱃지 추가 부착.
     - 행 배경에 은은한 틴트 효과(`bg-rose-50/30` 등) 적용.
   - 휴일/교통재해 등 특정 조건부 장해 담보는 일반 뱃지(`[기타]`)로 표기되고 핵심 뱃지에서 안전하게 제외.
4. **가족 핵심보장 비교 분석표(`J(I, _)`) 스코어링 규칙 정밀화**:
   - `injuryDisability`에서 `/휴일|교통|자가용|자전거/.test(...)`인 조건부 장해는 `score: 0`으로 완벽 제외.
   - `무재해사망특약`은 80%이상 고도장해로 `score: 90, label: "고도장해(80%↑)"`로 주요 핵심보장에 정확히 반영.
5. **버전 및 PWA 서비스 워커 갱신**:
   - `index.html`: `v=20260915_27`
   - `sw.js`: `family-insurance-cache-v27`


---

## [2026-09-15] 긴급 핫픽스: 세부특약 렌더링 함수 내 구문(Syntax) 괄호 밸런스 정상화 (v28)

### 1. 문제 발생 원인 분석
- v27 배포 중 증권 상세 모달(`PolicyListModal`) 내 `selectedPolicy.riders.map` 화살표 함수를 중괄호 본문(`{ return u.jsxs(...); }`) 형태로 변경하는 과정에서, 함수 종료부의 닫는 괄호 밸런스에 여분의 `)`가 남아 ESM 모듈 파싱 단계에서 `SyntaxError: Unexpected token ')'`가 발생함.
- 이로 인해 브라우저에서 번들 스크립트 로딩 시 구문 오류로 인해 화면이 멈추거나 렌더링되지 않는 현상 발생.

### 2. 해결 내용
- `assets/index-CLc_CX6I.js` line 1678의 화살표 함수 종료부 구문을 `}, r.id || idx); })`로 완전하게 정상화.
- Node.js `vm.Script` 파서 및 모듈 로더를 통해 ESM 번들 전체의 구문 무결성(Syntax Balance) 검증 완료 (`No syntax errors found`).
- `index.html` 캐시 버스팅 버전을 `v=20260915_28`로 갱신하고 `sw.js` 서비스 워커 캐시를 `family-insurance-cache-v28`로 업그레이드하여 사용자 브라우저에 즉시 정상 번들이 배포되도록 조치.


---

## [2026-09-15] 완벽 검증 베이스라인 기반 세부특약 렌더링 정상화 및 사이트 실행 복구 (v29)

### 1. 원인 분석 및 긴급 조치
- `selectedPolicy.riders.map` 렌더링 블록을 화살표 함수 블록 형태에서 원래의 검증된 표현식 화살표 함수(`(r, idx) => u.jsxs(...)`) 구조로 100% 원복.
- 표현식 구조를 해치지 않는 인라인 삼항 연산자 패턴으로 핵심보장 뱃지(`[상해후유장해]`, `[암]`, `[뇌질환]`, `[심장질환]`, `[실손의료비]`, `[핵심]`)와 배경색을 무결하게 주입.
- ESM 번들 로딩 및 Node.js `vm.Script` 파서 검증을 통해 구문 무결성(Syntax Balance 100%)을 실증 확인.
- 버전 캐시를 `v=20260915_29` 및 `family-insurance-cache-v29`로 업그레이드하여 배포 완료.


---

## [2026-09-15] 핵심보장비교표 1:1 일치 세부특약 로즈색 뱃지 시스템 구축 (v30)

### 1. 사용자 요구사항 및 배경
- **핵심보장비교표와의 1:1 시각적 동기화**:
  - 가입된 세부 특약 및 보장내용 목록에서 가족 핵심보장비교분석표에 로즈색 뱃지로 집계되는 1차 핵심보장 담보만 정밀하게 선별하여 로즈색 뱃지로 표기.
  - **암진단비**: 일반암 진단비에만 **`[암진단비]`** 로즈색 뱃지 적용.
  - **일반 암특약**: 유사암, 소액암, 소아암, 백혈병, 기타 암치료/방사선 담보는 기존처럼 차분한 **`[암]`** 회색 뱃지 유지.
  - **뇌혈관진단비**: 뇌혈관질환/뇌졸중/뇌출혈 핵심 진단비에 **`[뇌혈관진단비]`** 로즈색 뱃지 적용 (기타 뇌수술 등은 `[뇌질환]`).
  - **심장질환진단비**: 허혈성심질환/급성심근경색증 진단비에 **`[심장질환진단비]`** 로즈색 뱃지 적용 (기타 심장수술 등은 `[심장질환]`).
  - **상해후유장해**: 무재해상해특약, 무재해사망특약(80%↑), 일반상해후유장해에 **`[상해후유장해]`** 로즈색 뱃지 적용 (휴일/교통/자전거 조건부는 제외).
  - **질병후유장해 & 실손의료비**: 해당 핵심 담보에 각각 **`[질병후유장해]`**, **`[실손의료비]`** 로즈색 뱃지 적용.
  - 불필요하게 중복되던 특약명 옆의 작은 `[핵심]` 뱃지는 제거하여 시각적으로 정돈된 UI 완성.

### 2. 세부 구현 및 검증 내용
- `PolicyListModal` 내 인라인 IIFE를 통해 핵심보장비교표 집계 알고리즘과 100% 동일한 정규식 필터링 적용.
- 핵심보장 담보 행에만 은은한 로즈색 배경 틴트(`bg-rose-50/25`) 적용.
- Node.js `vm.Script` 및 ESM 모듈 파싱 테스트로 문법 무결성 100% 실증 검증 완료.
- 버전 캐시를 `v=20260915_30` 및 `family-insurance-cache-v30`으로 업그레이드하여 배포.


---

## [2026-09-15] 10대 핵심보장비교표 1:1 일치 세부특약 로즈 뱃지 전수 적용 및 GitHub 저장소 정리 (v31)

### 1. 사용자 요구사항 및 배경
1. **가족 10대 핵심보장비교표 항목과 1:1 일치하는 로즈색 뱃지 시스템 구축**:
   - 증권 상세 모달 내 "가입된 세부 특약 및 보장내용" 목록에서 가족 10대 핵심보장비교분석표에 로즈색 뱃지로 집계되는 **10가지 핵심 항목**을 정확히 추출하여 로즈색 뱃지(`bg-rose-100 text-rose-800 border-rose-200`)로 표기.
   - **암진단비**: 일반암 진단비만 **`[암진단비]`** 로즈 뱃지. 유사암, 소액암, 소아암, 백혈병, 특정암 등은 기존처럼 **`[암]`** 회색 일반 뱃지.
   - **뇌혈관질환진단비**: 뇌혈관질환 핵심 진단비만 **`[뇌혈관질환진단비]`** 로즈 뱃지. (기타 뇌졸중/뇌출혈/뇌수술은 차분한 일반 뱃지 유지)
   - **허혈성심질환진단비**: 허혈성심(장)질환 진단비만 **`[허혈성심질환진단비]`** 로즈 뱃지.
   - **암주요치료비**: 항암방사선, 항암약물, 표적항암, 중입자 등 주요치료 담보에 **`[암주요치료비]`** 로즈 뱃지.
   - **암주요치료생활비**: 암(유사암) 주요치료 생활자금/생활비 담보에 **`[암주요치료생활비]`** 로즈 뱃지.
   - **순환계질환치료비**: 순환계질환 주요치료, 2대질환 치료, 혈전용해 담보에 **`[순환계질환치료비]`** 로즈 뱃지.
   - **질병후유장해**: 질병으로 인한 후유장해(3%~ 또는 80%↑ 고도장해)에 **`[질병후유장해]`** 로즈 뱃지.
   - **상해후유장해**: 일반상해후유장해(3%~100%), 무재해상해, 무재해사망(80%↑ 고도장해)에 **`[상해후유장해]`** 로즈 뱃지. (휴일/교통재해 등 조건부 장해는 제외)
   - **질병수술비**: 질병 1~5종 수술비 및 질병수술 담보에 **`[질병수술비]`** 로즈 뱃지.
   - **상해수술비**: 상해 1~5종 수술비 및 상해수술 담보에 **`[상해수술비]`** 로즈 뱃지.
2. **GitHub 저장소 불필요 파일 정리 및 `.gitignore` 표준화**:
   - 워크스페이스 내 불필요한 임시 파일 방지 및 `.gitignore` 추가를 통해 깔끔한 깃 관리 상태 확립.

### 2. 세부 구현 및 검증 내용
1. **`PolicyListModal` 세부 특약 렌더링 내 10대 핵심보장 정밀 매핑**:
   - 인라인 IIFE 패턴을 유지하여 React 컴포넌트 트리와 구문 괄호 밸런스(Syntax Balance 100%) 무결성 유지.
   - 10대 핵심 항목에 해당할 경우에만 은은한 로즈색 배경 틴트(`bg-rose-50/25`) 적용.
2. **GitHub 저장소 청결화**:
   - 표준 `.gitignore` 파일 생성 및 반영: OS 캐시 파일(`.DS_Store`, `Thumbs.db`), `node_modules/`, `scratch/`, `*.tmp`, `*.log` 등 제외 처리.
3. **무결성 검증**:
   - Node.js `vm.Script` 및 ESM 모듈 파싱 테스트로 문법 오류 0건 실증 검증 완료.
4. **버전 및 PWA 서비스 워커 갱신**:
   - `index.html`: `v=20260915_31`
   - `sw.js`: `family-insurance-cache-v31`


---

## [2026-09-15] 핵심보장비교분석표 1:1 일치 정밀 필터링 및 보장별 전용 테마 컬러 뱃지 시스템 구축 (v32)

### 1. 사용자 요구사항 및 배경
1. **핵심보장비교분석표와의 엄격한 1:1 일치 (과도한 뱃지 부여 현상 해결)**:
   - 이전 버전에서 `암주요치료비` 항목에 항암방사선치료비, 항암약물치료비, 항암중입자방사선치료비 등이 모두 로즈 뱃지로 표기되어 비교분석표와의 일관성이 결여되는 문제 발생.
   - **핵심보장비교분석표 카드에서 `[핵심]` 태그와 붉은색 카드로 강조되는 진짜 핵심 담보(`isPrimary: true`)**에만 전용 컬러 뱃지를 부여하도록 기준 전면 재정비:
     - **암주요치료비**: `비급여 암 주요치료비` 등 핵심 주요치료 담보에만 뱃지 부착. (항암방사선, 항암약물, 중입자치료, 표적항암 등 일반 담보는 비교표와 동일하게 차분한 회색 일반 `[암]` 뱃지 유지)
     - **암진단비**: 순수 일반암 진단비만 뱃지 부착. (소액암, 유사암, 소아암, 백혈병, 특정암 등은 회색 일반 `[암]` 뱃지 유지)
     - **뇌혈관질환진단비**: 뇌혈관질환 진단비만 뱃지 부착. (뇌졸중, 뇌출혈, 양성뇌종양은 회색 일반 `[뇌질환]` 뱃지 유지)
     - **허혈성심질환진단비**: 허혈성심(장)질환 진단비만 뱃지 부착. (급성심근경색, 심혈관특정 등은 회색 일반 `[심장질환]` 뱃지 유지)
     - **상해후유장해**: 일반상해후유장해(3%~100%), 무재해상해, 무재해사망(80%↑ 고도장해)에만 뱃지 부착. (휴일/교통재해 등 조건부 장해는 일반 `[기타]` 유지)
     - **질병후유장해**: 질병후유장해(3%~100%) 및 질병 고도장해(80%↑)에만 뱃지 부착.
     - **질병수술비 & 상해수술비**: 각각 1~5종 수술비 및 기본 수술비 핵심 담보에만 뱃지 부착.
2. **보장 항목별 개별 테마 컬러(Distinct Colors) 도입**:
   - 모든 핵심 항목이 단일 로즈색이던 UI에서 벗어나, 보장의 성격에 맞게 10대 핵심보장별로 시각적으로 차별화된 세련된 색상 체계 적용:
     - **암진단비**: 로즈 레드 (`bg-rose-100 text-rose-800 border-rose-200`)
     - **뇌혈관질환진단비**: 우아한 퍼플 (`bg-purple-100 text-purple-800 border-purple-200`)
     - **허혈성심질환진단비**: 따뜻한 앰버 (`bg-amber-100 text-amber-800 border-amber-200`)
     - **암주요치료비**: 화사한 핑크 (`bg-pink-100 text-pink-800 border-pink-200`)
     - **암주요치료생활비**: 세련된 푸시아 (`bg-fuchsia-100 text-fuchsia-800 border-fuchsia-200`)
     - **순환계질환치료비**: 깊이 있는 인디고 (`bg-indigo-100 text-indigo-800 border-indigo-200`)
     - **질병후유장해**: 차분한 틸 (`bg-teal-100 text-teal-800 border-teal-200`)
     - **상해후유장해**: 신뢰감 있는 블루 (`bg-blue-100 text-blue-800 border-blue-200`)
     - **질병수술비**: 선명한 에메랄드 (`bg-emerald-100 text-emerald-800 border-emerald-200`)
     - **상해수술비**: 청량한 사이언 (`bg-cyan-100 text-cyan-800 border-cyan-200`)
     - **비핵심 일반 특약**: 차분한 슬레이트 회색 (`bg-slate-100 text-slate-600 border-slate-200`)

### 2. 세부 구현 및 검증 내용
1. **`PolicyListModal` 렌더링 함수 내 핵심 담보 정규화 완벽 적용**:
   - 세부보장명 내 공백(`\s*`) 정규식 지원 강화 (예: `뇌혈관질환 진단비`, `허혈성심장질환 진단비` 등 완벽 식별).
   - 질병후유장해와 상해후유장해 우선순위 분리 및 상호 오매칭 방지 로직 적용.
   - 2~3회에 걸친 전수 샘플 및 실제 데이터 정밀 스캔 테스트 통과.
2. **무결성 검증 및 GitHub 청결화**:
   - Node.js `vm.Script` 파서 및 ESM 동적 모듈 로딩 100% 무결성 검증 완료.
   - `.gitignore`를 통한 불필요 임시 파일 배제 유지.
3. **버전 및 PWA 서비스 워커 갱신**:
   - `index.html`: `v=20260915_32`
   - `sw.js`: `family-insurance-cache-v32`


---

## [2026-09-15] 핵심보장비교분석표-증권상세정보 1:1 완벽 일치 통일 컬러(로즈) 시스템 구축 (v33)

### 1. 사용자 요구사항 및 배경
1. **비교분석표(1번)와 증권상세정보(2번) 간의 100% 통일성 있는 컬러 강조 체계**:
   - 1번 그림인 **가족 핵심보장(10대 주요보장) 비교분석표**의 상단 범례(`● 핵심 보장 (은은한 붉은색)`) 및 붉은색 카드(`[핵심]`, `bg-rose-50/80 border-2 border-rose-200`)와 완벽하게 일치하도록,
   - 2번 그림인 **보험 증권 상세 정보**의 세부 특약 목록에서도 동일한 핵심 보장 항목들에 대해 통일성 있게 **선명한 로즈색 뱃지(`bg-rose-100 text-rose-800 border-rose-200`)와 은은한 붉은색 배경(`bg-rose-50/30`)**을 적용.
   - CSS 빌드 시 누락될 수 있는 비표준 Tailwind 유틸리티 클래스 대신, 번들 CSS에 완벽하게 내장된 검증된 로즈 테마를 활용하여 브라우저에서 100% 선명하고 일관되게 표시.
2. **2번 그림 속 핵심 담보들의 정확한 로즈 뱃지 매핑**:
   - `비급여(전액본인부담 포함) 암 주요치료비Plus`: **`[암주요치료비]`** 로즈 뱃지 + 은은한 붉은색 배경.
   - `암(유사암제외) 주요치료생활비`: **`[암주요치료생활비]`** 로즈 뱃지 + 은은한 붉은색 배경.
   - `유사암 주요치료생활비`: **`[암주요치료생활비]`** 로즈 뱃지 + 은은한 붉은색 배경.
   - `신특정순환계질환 주요치료비Plus`: **`[순환계질환치료비]`** 로즈 뱃지 + 은은한 붉은색 배경.
   - `항암방사선치료비`, `항암약물치료비`, `항암중입자방사선치료비`: 비교표와 동일하게 **`[암]`** 차분한 회색 일반 뱃지 유지.

### 2. 세부 구현 및 검증 내용
1. **`PolicyListModal` 렌더링 함수 내 로즈 테마 일괄 통일**:
   - 10대 핵심 보장 판정 알고리즘(`isCore`)을 비교분석표의 `isPrimary === true` 조건과 완벽하게 동기화.
   - 핵심 보장 항목은 선명한 로즈색 뱃지 및 `bg-rose-50/30` 행 배경으로 1:1 일치 강조.
2. **무결성 검증 및 GitHub 청결화**:
   - Node.js `vm.Script` 파서 및 ESM 동적 모듈 로딩 100% 무결성 검증 완료.
3. **버전 및 PWA 서비스 워커 갱신**:
   - `index.html`: `v=20260915_33`
   - `sw.js`: `family-insurance-cache-v33`


---

## [2026-09-15] KB라이프 뇌혈관·허혈성심장질환 주요치료 특약 순환계질환치료비 핵심 보장 전수 편입 및 동기화 (v34)

### 1. 사용자 요구사항 및 배경
- **KB라이프생명 순환계질환(2대질환) 주요치료 특약의 순환계질환치료비 핵심보장 편입**:
  - `뇌혈관·허혈성심장질환 주요치료Plus 특약` (1,000만원, 뇌혈관 또는 허혈성심장질환 수술 및 주요치료 보장)
  - `뇌혈관·허혈성심장질환 주요치료(각 연간 1회한) 특약(해약환급금 미지급형)` (300만원, 수술/혈전용해치료/중환자실치료 보장)
  - 위 2개 특약은 명칭에 "순환계"라는 단어가 직접 포함되어 있지 않아 기존에는 일반 `[뇌질환]` 또는 `[수술·입원]`으로 분류되었으나, 실질적으로 뇌혈관 및 허혈성심장질환(순환계 2대 질환)의 수술 및 주요치료를 전담 보장하는 핵심 담보임.
  - 사용자의 요청에 따라 가족 핵심보장비교분석표의 **`순환계질환(진단·치료비)`** 항목 및 증권 상세 모달의 **`[순환계질환치료비]`** 핵심 로즈 뱃지 체계에 전수 편입하고 일관된 붉은색 강조를 적용함.

### 2. 세부 구현 및 검증 내용
1. **가족 10대 핵심보장 비교분석표 (`buildFamilyComparisonData` -> `yP`) 동기화**:
   - 집계 필터 `O`: `/(순환계|심뇌혈관|2대질환|뇌혈관[·\s]*허혈성).*(주요치료|치료|진단)|뇌혈관.*수술|허혈.*수술|심혈관.*수술|혈전용해/` 로 확장하여 KB라이프 특약들이 6번 순환계질환치료비 열에 즉시 집계되도록 조치.
   - 스코어링 함수 `J`: `/(순환계|심뇌혈관|2대질환|뇌혈관[·\s]*허혈성).*(진단|주요치료)/` 일 때 `{score: 100, isPrimary: true, label: "핵심"}` 배정하여 비교분석표에서 은은한 붉은색 카드와 `[핵심]` 뱃지로 강조 렌더링.
2. **증권 상세 모달 (`PolicyListModal`) 동기화**:
   - `isCirc` 정규식에 `뇌혈관[·\s]*허혈성.*(주요치료|치료)` 패턴을 포함하여, 증권 상세정보 창에서도 해당 특약이 **`[순환계질환치료비]`** 선명한 로즈 뱃지(`bg-rose-100 text-rose-800 border border-rose-200`) 및 은은한 붉은색 행 배경(`bg-rose-50/30`)으로 완벽하게 강조되도록 적용.
3. **무결성 검증 및 버전 배포**:
   - Node.js `vm.Script` 구문 무결성 및 ESM 동적 모듈 로딩 오류 0건 확인.
   - `index.html`: `v=20260915_34`
   - `sw.js`: `family-insurance-cache-v34`


---

## [2026-09-15] 뇌출혈·급성심근경색(2대질환진단) 순환계 핵심치료비 제외 및 일반 질환 뱃지 환원 (v35)

### 1. 사용자 요구사항 및 배경
- **범위가 좁은 진단 특약(뇌출혈·급성심근경색)의 순환계질환치료비 핵심 강조 제외**:
  - 흥국생명 `(무)2대질환진단특약`(남욱)과 같이 보장내용이 **뇌출혈 및 급성심근경색증 진단**으로 한정된 특약은 포괄적인 뇌혈관질환이나 허혈성심장질환 전체를 포괄하지 못하는 좁은 범위의 담보임.
  - 사용자의 요청에 따라 이러한 단순 2대질환(뇌출혈·급성심근경색) 진단 특약을 순환계질환치료비 핵심 강조 대상에서 완전히 제외하고, 원래의 차분한 일반 **`[뇌질환]`** 또는 **`[심장질환]`** 회색 기본 뱃지(`bg-slate-100 text-slate-600 border border-slate-200`) 및 기본 투명 행 배경으로 정규화함.

### 2. 세부 구현 및 검증 내용
1. **증권 상세 모달 (`PolicyListModal`) 필터링 정교화**:
   - `isNarrowCardio = /2대질환.*진단/.test(n) || (/뇌출혈|급성심근경색/.test(n + d) && !/주요치료/.test(n));` 가드 조건을 추가하여 단순 뇌출혈·급성심근경색 진단 특약의 `isCirc` 오매칭을 원천 차단.
   - `(무)2대질환진단특약`은 원래의 일반 `[뇌질환]` 회색 뱃지로 깔끔하게 표시됨을 실증 검증 완료.
2. **가족 10대 핵심보장 비교분석표 (`yP` 및 `J`) 동기화**:
   - 집계 필터 `O` 및 스코어링 함수 `J`에서 2대질환 단순 진단 및 뇌출혈/급성심근경색 진단 담보를 제외하고, 실제 순환계/심뇌혈관 주요치료 및 수술 담보만 엄격하게 집계하도록 조치.
3. **무결성 검증 및 버전 배포**:
   - Node.js `vm.Script` 구문 무결성 및 ESM 동적 모듈 로딩 오류 0건 확인.
   - `index.html`: `v=20260915_35`
   - `sw.js`: `family-insurance-cache-v35`

---

## [2026-09-17] 삼성생명 리빙케어 무파워수술특약(1~5종) 및 생명보험 종수술 질병·상해 수술비 전수 편입 및 강조 (v36)

### 1. 사용자 요구사항 및 배경
- **삼성생명 무배당삼성리빙케어보험(김건형 님) 1~5종 수술비 누락 및 강조 부재 문제 해결**:
  - 1번 사진(삼성생명 증권) 보장내용 확인 결과: `무파워수술보장특약(1종 140,000원 ~ 5종 7,000,000원)`이 가입되어 있으며 최대 700만원 보장.
  - 하지만 2번 사진(9 질병수술비)과 3번 사진(10 상해수술비)에서 해당 수술비가 제대로 반영되지 않거나 상해수술비가 "미가입"으로 표시되고, 강조 표기가 누락되어 있었음.
- **약관 및 업계 표준 원리**:
  - 손해보험사는 질병 1~5종과 상해 1~5종을 별도로 분리하여 판매하지만, 삼성생명·흥국생명·한화생명 등 전통 생명보험사의 1~5종(또는 1~3종) 수술특약은 약관상 **질병 및 재해(상해) 수술을 모두 1~5종으로 동시 보장**함.
  - 따라서 종수술비는 비교 분석표의 **9 질병수술비**와 **10 상해수술비** 양쪽 모두에 완벽히 편입되어야 하며, 동일한 `[종수술]` 로즈 뱃지 및 은은한 붉은색 배경으로 강조되어야 함.

### 2. 세부 구현 및 검증 내용
1. **데이터 원본(CSV) 및 정규화 동기화**:
   - `assets/index-CLc_CX6I.js` 내 김건형 님 증권 데이터에서 삼성생명 `무파워수술특약`을 `무파워수술보장특약(1~5종 수술비)` (7,000,000원, 80세만기, 수술·입원)으로 업데이트.
   - 흥국생명 `(무)수술보장특약` (5,000,000원, 1~5종), 한화생명 `수술보장(본인)` (1,000,000원, 1~3종) 전수 정규화.
2. **핵심보장 비교분석표 (`yP`, `J`, `Y`) 로직 개편**:
   - **수술비 필터링 확장**: `S`(질병수술비)와 `F`(상해수술비) 필터에서 `/(1~[3578]종|종수술|무파워수술|수술보장)/` 패턴을 공통 편입하여 생명보험 종수술이 질병/상해 양쪽에 정상 노출되도록 개선.
   - **우선순위 스코어링(`J`) 통일**: `[종수술]`(score: 100) 및 `[기본수술]`(score: 80~90)에 `{isPrimary: true}`를 부여하여 질병수술비와 상해수술비 카드 모두 로즈 뱃지와 붉은 테마로 일관되게 강조.
   - **복합 특약 산정 정밀화(`Y`)**: 김건형 님의 `무상해치료특약`에서 재해수술 보장금액 100만원을 정확히 추출하여 상해수술비 합산(종수술 700만 + 재해수술 100만 = 800만원)에 완벽 반영.
3. **증권 상세 모달 (`PolicyListModal`) 뱃지 정규화**:
   - 종수술 관련 특약(`1~5종`, `무파워수술`, `수술보장` 등)에 대해 **`[종수술]`** 선명한 로즈 뱃지(`bg-rose-100 text-rose-800 border border-rose-200`) 및 은은한 붉은색 행 배경(`bg-rose-50/30`) 적용.
4. **무결성 검증 및 결과 확인**:
   - `node -c assets/index-CLc_CX6I.js`: 구문 무결성 통과 (0 error).
   - 비교분석표 검증 결과:
     - 김건형 [9 질병수술비]: 3,720만원 (★[종수술] 삼성생명 700만원 반영 완료)
     - 김건형 [10 상해수술비]: 800만원 (★[종수술] 삼성생명 700만원 + ★[기본수술] 100만원, '미가입' 완전 해소)
     - 남욱 [9 질병수술비]: 1,420만원 (★[종수술] 흥국생명 500만원 + ★[종수술] 한화생명 100만원 정상 반영)
     - 남욱 [10 상해수술비]: 637만원 (★[종수술] 흥국생명 500만원 + ★[종수술] 한화생명 100만원 정상 반영)
     - 자녀 3인(남검재, 남차율, 남주하) 기존 수술비 데이터 100% 무손실 보존.
5. **PWA 캐시 및 배포 버전 갱신**:
   - `index.html`: `v=20260917_36`
   - `sw.js`: `family-insurance-cache-v36`

---

## [2026-09-17] 가입증권 상세 모달 렌더링 런타임 오류(isDSurg is not defined) 긴급 핫픽스 (v37)

### 1. 현상 및 원인
- **현상**: '가입증권&관리인원' 탭에서 특정 보험 증권을 클릭하여 상세 모달(`PolicyListModal`)을 열 때 "화면을 불러오는 중 문제가 발생했습니다: isDSurg is not defined" 오류 발생.
- **원인**: 특약별 뱃지 레이블을 렌더링하는 `children: (() => { ... })()` 내부 스코프에 `isDSurg` 및 `isISurg` 변수 선언이 누락된 상태에서 `if (isDSurg && isISurg ...)` 구문이 실행되어 자바스크립트 `ReferenceError` 발생.

### 2. 해결 및 검증
- **수정 내용**: `children` 클로저 내부에도 `isDSurg` 및 `isISurg` 정규식 판정 변수를 완벽히 보강.
- **검증**:
  - `node -c assets/index-CLc_CX6I.js`: 구문 무결성 통과 (0 error).
  - 507개 전체 특약에 대한 증권 모달 렌더링 시뮬레이션 테스트 실행 (`verify_modal.js`): 오류 0건 통과.
- **배포 및 캐시 갱신**:
  - `index.html`: `v=20260917_37`
  - `sw.js`: `family-insurance-cache-v37`

---

## [2026-09-17] 전체 계약기간(보험기간) 공란 해소 및 만기일자(2번 사진 형태) 전수 동기화 (v38)

### 1. 현상 및 원인
- **현상 1**: 가입증권 상세 모달에서 `전체 계약기간 (보험기간)` 필드가 공란으로 표시됨.
  - **원인**: `PolicyListModal`에서 `selectedDateInfo.fullPeriodStr`를 참조하여 렌더링하고 있었으나, `getPolicyDateInfo` 함수의 반환 객체에 `fullPeriodStr` 속성이 누락되어 `undefined`가 반환됨.
- **현상 2**: 일부 증권에서 `만기일자 (보장만기)`가 `(100세만기)`처럼 날짜 없이 괄호만 표기되는 현상 발생.
  - **원인**: 개별 특약 행에 생년월일이 누락되거나 날짜 포맷이 다양할 경우 `birthDate`가 null이 되어 만기 날짜(`YYYY-MM-DD`)를 연산하지 못하고 만기 텍스트만 출력됨.

### 2. 해결 및 세부 구현
1. **가족 기본 생년월일 사전(`FAM_BIRTH_MAP`) 내장**:
   - 피보험자(김건형: 1976-10-28, 남욱: 1975-07-27, 남검재: 2012-06-07, 남차율: 2014-06-05, 남주하: 2016-09-20)의 생년월일 딕셔너리를 구축하여, 특약 데이터에 생년월일이 비어있더라도 100% 만기일자를 정확하게 연산.
2. **만기일자 표기(2번 사진 형태) 표준화**:
   - `2116-09-20 (100세만기)`와 같이 **만기일자(YYYY-MM-DD)와 괄호 안의 만기 기준**이 함께 완벽하게 표기되도록 정규화.
3. **전체 계약기간(보험기간) 산출 및 공란 해소**:
   - `fullPeriodStr`: 시작일과 만기일, 그리고 총 계약 연수를 함께 표기 (예: `2026-09-11 ~ 2116-09-20 (90년)`, 종신인 경우 `2005-12-27 ~ 2056-12-27 (51년 / 주보험 종신)` 또는 `2008-04-01 ~ 종신`).
   - `PolicyListModal` 렌더링에서 `selectedDateInfo.fullPeriodStr || selectedDateInfo.dateRangeStr || "-"` 안전 fallback 적용.
4. **전체 22개 보험 증권의 계약일자/만기일자 전수 검증 통과**:
   - 로컬 CSV (22개 증권) 및 구글 스프레드시트 클라우드 CSV (26개 증권) 대상 전수 검증 결과 오류 0건 통과.
5. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260917_38`
   - `sw.js`: `family-insurance-cache-v38`


---

## [2026-09-18] 모던 SaaS / 핀테크 스타일 반응형 UI/UX 전면 개편 (v39)

### 1. 개편 배경 및 목표
- **목표**: 기존의 다소 투박하고 요란한 원색 바텀 그라데이션 및 일관성 없는 카드 여백을 걷어내고, 현대적인 SaaS / 핀테크(토스, 뱅크샐러드) 감성의 정갈하고 미니멀한 UI/UX 구축.
- **반응형 최적화**: 모바일(2열 카드 / 컴팩트 서체)과 PC(4열 와이드 카드 / 여유로운 패딩) 환경에서 각각 최적의 정보 밀도와 가독성을 제공.
- **안전장치**: 개편 전 상태로 언제든 1초 만에 복귀할 수 있도록 Git 태그 `v38-stable`를 생성하여 GitHub 원격에 안전 보존.

### 2. 세부 UI/UX 개선 내역
1. **대시보드 KPI 카드 (`b7`) 미니멀 핀테크 디자인 개편**:
   - **그라데이션 막대 제거**: 하단의 촌스러운 원색 바텀 그라데이션 라인 전면 제거.
   - **카드 비주얼 고도화**: 은은한 보더(`border-slate-200/90`), 섬세한 그림자(`shadow-2xs`), 호버 인터랙션(`hover:border-slate-300 hover:shadow-xs`) 적용.
   - **수치 가독성 극대화**: 고정폭 숫자(`tabular-nums tracking-tight font-black`)로 레이아웃 흔들림 방지 및 숫자의 시인성 확보.
   - **가입증권 & 인원 카드**: 클릭 가능한 마이크로 뱃지(`[조회]`), 우측 화살표 애니메이션(`상세보기 →`), 모바일 터치 피드백(`active:scale-[0.98]`) 적용.
   - **모바일/PC 반응형 그리드**: 모바일 2열(`grid-cols-2 gap-2.5`), PC 4열(`lg:grid-cols-4 gap-4`) 및 반응형 폰트/패딩 조율.
2. **보장 카테고리 타일 그리드 (`C7`) 정돈**:
   - 불투명도와 점선 테두리(`border-dashed`)를 현대적인 화이트/슬레이트 카드 타일로 정돈하여 직관적이고 깔끔한 비주얼 구현.
3. **PWA 캐시 및 브라우저 강제 갱신 적용**:
   - `index.html`: `v=20260918_39`
   - `sw.js`: `family-insurance-cache-v39`

---

## [2026-09-18] 옵션 1 적용: PC 상단 일체형 고정 & 모바일 하단 탭바 + 스크롤 그림자 & 반투명 블러 (v40)

### 1. 개편 배경 및 목표
- **사용자 제안 및 개선 목표**:
  - 긴 보험 특약 목록 스크롤 시 상단 메뉴와 가족 탭이 사라져 재스크롤해야 하는 불편 해소.
  - PC에서는 상단 헤더와 가족 탭 바가 일체형으로 고정(`sticky top-0`)되어 한눈에 조작 가능하도록 구축.
  - 모바일에서는 화면 상단 공간을 절약하기 위해 슬림 1단 헤더(48px) + 가족 탭 바를 상단 고정하고, 핵심 액션 메뉴(동기화, 핵심보장, AI분석, 수기등록, 설정)는 엄지손가락이 닿기 쉬운 **모바일 하단 내비게이션 바**로 분리 배치.
  - 스크롤 시 부드러운 반투명 블러(`backdrop-blur-md`)와 스크롤 시에만 활성화되는 스크롤 그림자(`Elevation Shadow`)를 결합하여 현대적인 웹앱 비주얼 완성.

### 2. 세부 구현 내역
1. **스크롤 감지 동적 그림자 (Elevation Shadow)**:
   - `window.scrollY > 8` 감지 시 상단 고정 헤더에 `shadow-sm border-slate-200`가 동적으로 활성화되고, 최상단에서는 `shadow-none border-slate-200/60`으로 전환되는 부드러운 애니메이션 적용.
2. **반투명 블러 효과 (Backdrop Blur)**:
   - 상단 헤더(`bg-white/90 backdrop-blur-md`), 가족 탭 바(`bg-white/90 backdrop-blur-md`), 모바일 하단 바(`bg-white/95 backdrop-blur-md`) 전면에 적용하여 스크롤 시 컨텐츠가 뒤로 은은하게 비치도록 구현.
3. **모바일 하단 탭 바 (Mobile Bottom Bar)**:
   - 최신 모바일 OS 가이드라인에 맞춰 `pb-[max(env(safe-area-inset-bottom),0.5rem)]`을 적용하여 아이폰 홈 인디케이터 터치 충돌 원천 방지.
   - 중앙 'AI 증권 분석' 버튼을 볼록한 플로팅 그라데이션 버튼으로 강조.
   - 본문 `main` 태그에 `pb-24 sm:pb-6` 안전 마진을 적용하여 최하단 컨텐츠 가림(Clipping) 결함 완전 해소.
4. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260918_40`
   - `sw.js`: `family-insurance-cache-v40`

---

## [2026-09-18] 가족 이름 탭 바 단독 최상단 고정 (Sticky Top-0) 적용 (v41)

### 1. 개편 배경 및 목표
- **사용자 제안 및 개선 목표**:
  - 상단 헤더(로고)와 가족 탭 바가 둘 다 고정되어 상단 여백을 많이 차지하는 답답함 해소.
  - 헤더(로고)는 스크롤 시 위로 자연스럽게 올라가고, **가장 자주 조작하는 [가족 이름 탭 바]만 화면 맨 위(`top-0`)에 찰떡처럼 단독 고정**되도록 정돈.
  - 상단 고정 높이가 50px대로 대폭 슬림해져 본문 보험 정보 가시 영역(Viewport)이 2배 이상 넓어짐.

### 2. 세부 구현 내역
1. **상단 헤더 (`p7`)**: 일반 헤더로 변경하여 스크롤 시 자연스럽게 위로 올라감.
2. **가족 이름 탭 바 (`y7`)**: `sticky top-0 z-30 bg-white/95 backdrop-blur-md border-b border-slate-200/90 shadow-xs` 적용하여 화면 최상단에 항상 고정.
3. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260918_41`
   - `sw.js`: `family-insurance-cache-v41`

---

## [2026-09-18] 로고&타이틀 헤더 및 가족 이름 탭 바 상단 일체형 동시 고정 적용 (v42)

### 1. 개편 배경 및 목표
- **사용자 제안 및 개선 목표**:
  - 로고&타이틀(헤더)과 가족 이름 탭 바가 한 몸처럼 상단에 항상 동시 고정되어 언제든 브랜딩, 설정 메뉴, 가족 전환을 스크롤 위치와 상관없이 안정적으로 제어할 수 있도록 구축.
  - 모바일에서는 슬림 1단 헤더(48px) + 가족 탭 바가 상단에 딱 붙어 겹침 없이 고정되고, PC에서는 와이드 헤더(64px) + 가족 탭 바가 완벽하게 일체형으로 고정.

### 2. 세부 구현 내역
1. **상단 헤더 (`p7`)**: `sticky top-0 z-30 bg-white/90 backdrop-blur-md border-b border-slate-200/80 shadow-2xs` 적용.
2. **가족 이름 탭 바 (`y7`)**: 모바일 헤더 높이에 맞춘 `sticky top-12 sm:top-16 z-20 transition-all duration-200 shadow-xs` 적용하여 겹침 오차 0px로 헤더 바로 아래 착 달라붙어 동시 고정.
3. **모바일 하단 탭 바**: 엄지손가락 인체공학 5대 메뉴(동기화, 핵심보장, AI분석, 수기등록, 설정) 유지.
4. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260918_42`
   - `sw.js`: `family-insurance-cache-v42`

---

## [2026-09-18] 헤더 & 가족 탭 바 통합 래퍼(Unified Sticky Wrapper) 구조적 고정 (v43)

### 1. 원인 분석 및 긴급 해결
- **문제점**: CSS 번들에 `top-12`, `top-16` 유틸리티 클래스가 미존재하여 브라우저에서 가족 탭 바의 `top` 위치가 `auto`로 계산되어 스크롤되어 버리던 결함 규명.
- **해결 조치**:
  - `p7`(로고/헤더)과 `y7`(가족 탭 바)을 상위에서 단일 통합 래퍼 `div` (`style={{position: "sticky", top: 0, zIndex: 30}} className="sticky top-0 z-30 bg-white/95 backdrop-blur-md shadow-xs"`)로 감싸 구조적 일체화.
  - 두 요소가 한 몸으로 묶여 `top-0` 하나로 화면 최상단에 100% 무조건 동시 고정되도록 완벽 조치.
2. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260918_43`
   - `sw.js`: `family-insurance-cache-v43`

---

## [2026-09-18] 모바일 5대 핵심 메뉴 하단 탭바 뷰포트 최하단 분리 고정 완료 (v44)

### 1. 원인 분석 및 긴급 조치
- **문제점**: 모바일 5대 핵심 메뉴(`<nav>`)가 상단 헤더(`p7`)의 자식으로 렌더링되고 있었고, 상단 래퍼의 `backdrop-filter`에 의해 `position: fixed`의 기준 컨테이너가 뷰포트가 아닌 상단 래퍼로 갇혀 상단 가족 탭 바로 아래에 부착되는 현상 발생.
- **해결 조치**:
  - `p7` 내부에서 모바일 `<nav>`를 완전히 분리 제거.
  - 최상위 앱 레이아웃(`min-h-screen flex flex-col`)의 최하단에 `<nav style="position: fixed; bottom: 0; left: 0; right: 0; zIndex: 50" className="sm:hidden fixed bottom-0 left-0 right-0 z-50 ...">`를 배치하여 뷰포트 기준 스마트폰 화면 맨 아래에 100% 안착.
  - 상단에는 오직 [로고&타이틀 헤더 + 가족 이름 탭 바]만 일체형 고정.
2. **배포 및 캐시 갱신**:
   - `index.html`: `v=20260918_44`
   - `sw.js`: `family-insurance-cache-v44`
